# DESIGN_SYSTEM.md — PodsNow. のデザインシステム（Design system 2）

> 凡例: **【事実】** 決定した仕様、またはこのリポジトリで確認した現状 / **【確認済み】** 公式ドキュメント等で確認済み（出典付き） / **【仮説】** 未検証・要実機確認

画面の見た目と、それを決める値を「どこに・どういう名前で・どうやって決めて置くか」。
値そのものは `src/ui/tokens/` にあり、色は `scripts/design/ramps.py`、ロゴは `scripts/brand/geometry.py`、
書体は `scripts/fonts/generate.py` から生成する。矛盾したら、コードではなくここを先に直す提案をする（AGENTS.md）。

【事実】Issue #94 で、暫定デザイン（金 + ミント、マイク + レベルメーターのマーク）を全面刷新した。
設計成果物（受け渡しパッケージ `PodsNow-Design`、2026-09-22）を、#93 で決まった現在の情報設計
（録音 / 編集 / 書き出しの 3 タブ、マーカー廃止と「言い直す」）へ統合している。統合の判断と検証の記録は
`docs/design-refresh/README.md`。

表示ロゴは **PodsNow.**、読みはポッズナウ、文章中の製品名は PodsNow。識別子（リポジトリ、bundle id、
scheme、DB、`.podsnow`）は小文字の `podsnow` のまま変えない。

## 1. 何のためのデザインか

### 1.1 目指す体験【仮説】

**思いついた話を、迷わず録り始め、安心して整え、配信に渡せる。**

PodsNow は「スマートフォンの小さな DAW」ではなく、ポッドキャストを 1 本完成させるための道具。
録音中は話の内容に集中でき、編集時には必要な精度まで掘り下げられる。

### 1.2 守るプロダクトの前提【事実】

| 前提 | 根拠 | デザインへの影響 |
|---|---|---|
| ローカルファースト、アカウントなし | PRODUCT §6、NFR-2/5 | ログイン、クラウド同期の印、架空のオンライン状態を置かない |
| MVP は 1 番組 | FR-SHOW-1 | 番組切替、チーム、ワークスペースを置かない |
| 元の音声を壊さない編集 | FR-SAFE-7/8 | カットは Undo で戻せる。音声の物理削除とは明確に分ける |
| 声 1 本 + 素材レイヤー | FR-EDIT-1 | 多人数収録や自由配置 DAW を描かない |
| 配信 API 連携は MVP 外 | PRODUCT §7 | 「公開」ではなく「書き出し」「共有」「コピー」 |
| Dark / Light / System、日英 | FR-SET-1、FR-I18N-1 | ダークを主表現にするがライトを削らない |
| 1 エピソード = 1 画面、3 タブ | FR-EP-5、docs/ux-restructure.md §3 | 工程ごとの画面を作らない。タブで切り替える |

### 1.3 判断の優先順位【仮説】

1. 音声が守られることと、今の状態を正しく伝えること。
2. 次の操作が分かり、片手で届くこと。
3. 日本語・英語・大きい文字でも破綻しないこと。
4. タイポグラフィと間隔が製品の個性になること。
5. 装飾は上の 4 つを妨げない範囲に留めること。

## 2. アートディレクション【仮説】

**グラファイトの面、明るい文字、ひとつのシトロン。**

毎日触る精度の高いスタジオ機材をイメージする。説明のつかない発光、ガラス、粒子、宇宙、
ノイズテクスチャ、紫の大面積グラデーションは使わない。

| 要素 | 規則 | 排除する表現 |
|---|---|---|
| キャンバス | 低彩度のグラファイト。背景・面・操作面を段階化 | 真っ黒一色、紫のグラデーション |
| 主操作 | シトロンの塗り、濃い文字。1 画面に原則 1 つ | すべてのリンクやカードをアクセント化 |
| 文字 | 高密度のロゴ、読みやすい本文、等幅の時間 | 見出しごとのフォント変更、極細の本文 |
| レイアウト | 整列、余白、機能に合わせた密度 | 全項目のカード化、意味のない KPI |
| 波形 | 編集に必要な時だけ、大きく表示 | 音声が無い場所の飾りの波形 |
| 動き | 押下・状態変化への短い反応 | 常時点滅する REC、跳ねるボタン |

### 2.1 使わない定型（AI 生成に見える UI）【事実】

#96 の監査で、生成された UI に繰り返し現れる形として次を外した。新しい画面でも使わない。

| 定型 | 代わりにすること |
|---|---|
| 見出しの上に置く小さな分類ラベル（アイブロウ。「あなたの番組」「制作中」） | 見出しそのものを具体的にする。分類が要るなら見出しの横か下の補足にする |
| 英字の全部大文字ラベル（`YOUR SHOW`、`VOICE`） | 文頭だけ大文字（`Your show`）。`textTransform` も使わない |
| 完了画面の冒頭の丸いアイコン＋大見出し＋リード文 | ヘッダーのタイトルで場所を示し、すぐに結果（ファイル）と次の操作を出す |
| 角丸の箱の片側（下辺・左辺）だけに引く色の線 | 選択は面の色・輪郭 1 周・文字の強さで示す。片側の帯を足さない |
| ボタンの中の「→」 | 動詞のラベルだけ。行の末尾のシェブロン（移動する行）は OS の慣習として残す |
| 画面冒頭の説明リード文 | 必要な事実だけを該当する行や欄の補足に置く。無ければ書かない |

### 2.2 文言の書き方【事実】

- 平叙の説明にする。キャッチコピー調（読点で区切る「〜を、ここから。」、「〜へ。」の体言止め）、
  呼びかけ（「〜しましょう」「大丈夫」）、感嘆を使わない。
- 日本語は見出し・ボタン・ラベル・1 文だけの説明に句点を付けない。2 文以上の本文だけ句点で区切る。
- 英語は全部大文字にしない（略語を除く）。`src/i18n/__tests__/catalog.test.ts` が上の規則の一部を検査する。
- 1 つの画面で同じことを 2 回書かない（ヘッダーの補足とリード文、など）。

## 3. ロゴタイプとアプリアイコン

### 3.1 造形【事実】

- ロゴは **サービス名そのもの** `PodsNow.`。Manrope ExtraBold（800）の字形を土台に、
  `Po / od / ds / sN / No / ow` の字間を光学調整し、末尾の点を**独立した角丸正方形**として描く。
  文字全体を新規作字したとは称さない（Manrope は SIL OFL 1.1）。
- 通常の Text で打ち直して近似しない。アプリ内のロゴも輪郭（SVG パス）で描く（`src/ui/Wordmark.tsx`）。
- アイコンは `Pods` / `Now.` の **2 段全文**。マイク・波形・雪・電波・P/N の頭文字マークに戻さない。
- 点は短い発話の句点と「1 回を完成させる」感覚を担う。録音ランプ・通知・エラーに転用しない。

| 形 | 用途 | 生成物 |
|---|---|---|
| 横組み `PodsNow.` | Home 上部、スプラッシュ、ストア | `assets/brand/wordmark-{dark,light,mono}.svg`、`src/ui/brand/wordmark.ts`、`assets/images/splash-icon.png` |
| 2 段 `Pods` / `Now.` | iOS アイコン | `assets/brand/app-icon.svg`、`assets/images/icon.png` |
| 2 段（安全域） | Android アダプティブ前景・単色 | `android-foreground.svg`、`android-monochrome.svg`、`android-icon-{foreground,monochrome}.png` |
| 2 段（小サイズ） | favicon など 32px 以下 | `icon-small.svg`、`favicon.png` |

### 3.2 色・余白【事実】

- 横組み: 文字 `textPrimary`、点 `accentText`（ダークはシトロン、ライトは濃いオリーブ）。
- アイコン: 背景 `accentSolid`、文字と点 `accentOnSolid`。単色版は黒。
- アイコンに独自の角丸を焼き込まない（OS がマスクする）。Android 前景は中央 66% の安全域に収める。
- Home には 1 回だけロゴを出す。エピソード画面の上部はエピソード名に使う。

### 3.3 生成経路【事実】

```sh
python3 scripts/brand/generate.py        # 標準ライブラリのみ。SVG・PNG・アプリ内ロゴのデータを書く
python3 scripts/brand/extract_glyphs.py path/to/Manrope[wght].ttf   # 字形を変えるときだけ（fontTools が必要）
```

- 図形の正は `scripts/brand/geometry.py`（字間・点・配置）と `scripts/brand/glyphs.py`（Manrope 800 の輪郭。生成物）。
- 色はトークンの生成元 `ramps.py` をそのまま読む。`app.json` の背景色（スプラッシュ `#111416`、
  アダプティブ背景 `#D8F36A`）がトークンとずれていれば `generate.py` が失敗する。
- 受け渡しパッケージの `build_brand.py` と同じ字間・配置を再現しており、SVG の差分は描画して 0.1% 未満
  （`docs/design-refresh/README.md` §5）。受け渡しパッケージの生成元はリポジトリに置かない（生成元を 2 つにしない）。

## 4. タイポグラフィ

### 4.1 書体【事実】

| 用途 | 書体 | 同梱するウェイト |
|---|---|---|
| 欧文 UI（英語表示） | Manrope | 400 / 500 / 600 / 700 |
| 和文 UI（日本語表示） | Noto Sans JP | 400 / 500 / 600 / 700 |
| 時間・計測値 | IBM Plex Mono | 400 |
| ロゴ輪郭 | Manrope 800（輪郭として焼き込み。書体としては同梱しない） | — |

- すべて SIL OFL 1.1。原本・出典・ライセンス文は `assets/fonts/README.md` と `assets/fonts/*-OFL.txt`。
- React Native は可変フォントの軸を指定できないので、**使う太さだけを静的 TTF に切り出して同梱**する
  （`scripts/fonts/generate.py`、fontTools が必要）。疑似太字に頼らない。等幅は 400 だけを使う（テストで確認）。
- 起動時に外部 CDN から取得しない。ビルド時に `expo-font` の config plugin で埋め込む（`app.json`）。

### 4.2 役割尺度【事実】

単位は logical px / sp。固定サイズで文字拡大を抑止しない（`maxFontSizeMultiplier` を使わない）。

| 役割 | サイズ / 行高 | 太さ | 用途 |
|---|---|---:|---|
| `timer` | 48 / 58 | 400 mono | 収録中の時間。幅 360 未満では `timerCompact` 40 / 48 |
| `clock` | 32 / 40 | 400 mono | 編集の再生位置。狭い画面では `clockCompact` 24 / 32 |
| `display` | 32 / 40 | 700 | 番組名 |
| `title` | 24 / 34 | 700 | 画面の大見出し |
| `heading` | 20 / 28 | 600 | セクション見出し（#96 で 18 から上げ、本文との段差を広げた） |
| `body` / `bodyStrong` | 16 / 26 | 400 / 600 | 本文、トークテーマ |
| `label` | 14 / 21 | 600 | ボタン、設定項目 |
| `caption` | 13 / 20 | 500 | 日時、補助情報 |
| `overline` | 12 / 18 | 600 | 波形のレーン名など、図の中の短い名札。見出しの上には置かない（§2.1） |
| `mono` | 14 / 20 | 400 mono | 行の中の時間・サイズ |
| `tick` | 11 / 14 | 400 mono | 波形の目盛りだけ |

- 動く時間は tabular numbers。収録中は `mm:ss`、1 時間以上は `h:mm:ss`（`formatClock`）。小数は出さない。
- 操作ラベルは折り返せる（Button は 1 行に固定しない）。

### 4.3 書体の選び方と退避【事実】

- React Native には Web のフォールバック列が無い。**UI の書体はロケールで 1 つ選ぶ**（日本語 → Noto Sans JP、
  英語 → Manrope）。ユーザー入力の和欧混植は OS のグリフ代替に任せる【仮説: 実機で確認（DS-1）】。
- 画面・`features/`・`ui/` は `react-native` の Text / TextInput を直接使わず、`@/ui/components` の
  `Text` / `TextInput` を使う（ESLint で強制）。これがロケールに応じて `fontFamily` を足す。
- **書体の読込失敗で起動・収録を止めない。** `expo-font` の `getLoadedFonts()` に書体が無ければ
  `fontFamily` を付けず OS 書体に退避する（等幅は Menlo / monospace）。判定は `src/ui/fonts.ts`（テストあり）。
- 【事実】Noto Sans JP は 1 ウェイト約 5.8 MB、4 ウェイトで約 23 MB がアプリに加わる。和文の実ウェイトを
  揃えるための代償として受け入れた。縮める案（例: Android では OS の Noto Sans CJK を使う）は
  `docs/design-refresh/README.md` §7 の残件。

## 5. 色の体系

### 5.1 基本色【事実】

| 役割 | Dark | Light | 用途 |
|---|---|---|---|
| `bg` | `#111416` | `#F1F3EF` | キャンバス |
| `surface` | `#1B2023` | `#FFFFFF` | カード、波形パネル |
| `surfaceRaised` | `#252B2F` | `#E7ECE5` | シート、通知、無効ボタン |
| `surfaceHover` | `#30373B` | `#DFE5DC` | 押下、選択セグメント |
| `textPrimary` | `#F1F3EF` | `#18201C` | 本文、見出し |
| `textSecondary` | `#BEC7CB` | `#404D44` | 補助情報 |
| `textTertiary` | `#A6B1B7` | `#4D5B50` | 目盛り、低優先情報 |
| `textDisabled` | `#8B979D` | `#687568` | 無効状態の文字・アイコン |
| `border` | `#3D454A` | `#C4CDC5` | 装飾の区切り |
| `borderStrong` | `#849096` | `#717B71` | 入力欄、操作の輪郭 |
| `accentSolid` | `#D8F36A` | `#D8F36A` | 主操作の塗り |
| `accentOnSolid` | `#172007` | `#172007` | シトロン上の文字 |
| `accentText` | `#D8F36A` | `#3D500C` | 選択された文字、ロゴの点 |
| `accentBorder` | `#BBD44F` | `#5B721F` | 選択の輪郭、ライトの主操作の輪郭 |
| `focusRing` | `#D8F36A` | `#4A6212` | 入力中の輪郭 |

ライトではシトロンの塗りが白地に 3:1 を持てないので、主操作・トグル・再生ヘッド・選択のハンドルは
`accentBorder` の輪郭（または線色）で形を示す。塗りの色だけを反転する実装をしない。

### 5.2 情報に使う色【事実】

| 意味 | トークン | Dark の塗り | 必ず併用する形・文字 | 禁止 |
|---|---|---|---|---|
| 録音 | `rec*` | coral `#FF896A` | 「収録中」、開始は丸、停止は四角 | 削除・警告への流用 |
| 破壊的操作 | `danger*` | rose `#F59CB4` | ゴミ箱、「削除」 | 録音・カットへの流用 |
| 声 | `voice*` | mineral `#D7E0E4` | 「声」レーン | 完了と同義にする |
| BGM | `music*` | lilac `#B9B1FA` | 素材名、BGM | 主操作 |
| 差し込み素材 | `insert*` | blue `#88D0F4` | 素材名、クリップ枠 | 補助操作全般 |
| 注意・割り込み | `mistake*` | amber `#F4C087` | 注意三角、文言 | エラーと録音を混ぜる |
| 完了 | `success*` | green `#8ED8B4` | チェック、「書き出しました」 | 保存未完了で出す |

**`voice = success` は解消した。** 声が録れていても保存・仕上げ・公開が完了したとは限らないため。
テストが「別の色であること」を確かめる。録音（コーラル）と破壊（ローズ）も別の色で、テストで確かめる。

色相の間隔は 30° 以上（`generate.py` が検査）。旧仕様の 40° から下げた。コーラル 35.7° とローズ 2.4°・
アンバー 68.5° の間が 33° のため。意味は色だけで運ばず、形と文字を必ず併せる（§9、§10）。

### 5.3 値は計算する【事実】

`scripts/design/ramps.py` は OKLCh で色を作る。

- **面と塗り**は OKLCh（明度・彩度・色相）を決め打ちする。設計の hex をそのまま再現する値。
- **文字と境界**は彩度と色相だけを決め、明度を**目標コントラスト比から逆算**する。目標比は設計値が
  実際に持っていた比（小数第 2 位で切り捨て）。面を動かせば文字が追従し、読めない組み合わせが残らない。
- `generate.py` は書き出す前に 246 組を測り、ひとつでも落ちたら何も書かない。`src/ui/__tests__/tokens.test.ts`
  が生成物を測り直す（生成を忘れて `colors.ts` を手で直したときに CI で落とす）。

基準【確認済み】:

| 対象 | 比 | 出典 |
|---|---|---|
| 本文・ラベル | 4.5:1 | https://www.w3.org/TR/WCAG22/#contrast-minimum |
| 操作部品の輪郭・状態を示す塗り・焦点 | 3:1 | https://www.w3.org/TR/WCAG22/#non-text-contrast |
| 無効状態 | 3:1（要件外の製品基準。見えなくはしない） | — |

これは配色の部分検証で、アプリ全体の WCAG 適合を宣言するものではない。

### 5.4 設計値との差【事実】

逆算の二分探索の丸めで、次の 6 値が設計（`tokens.json`）と sRGB の 1 段だけ違う。見た目の差は無く、
いずれも目標比を満たす。設計値に揃えるために逆算をやめることはしない。

| テーマ | トークン | 設計 | 生成 |
|---|---|---|---|
| dark | `successText` | `#ABE4C8` | `#ABE4C9` |
| light | `accentText` | `#3D500B` | `#3D500C` |
| light | `focusRing` | `#4A6211` | `#4A6212` |
| light | `musicBorder` | `#7B68A7` | `#7B68A8` |
| light | `mistakeText` | `#6D430C` | `#6D430D` |
| light | `mistakeBorder` | `#93682F` | `#936830` |

`overlayScrim`（モーダル背面）と `selectionOverlay` / `recordingOverlay`（波形に重ねる帯）は設計に無く、
旧仕様の決め方（黒の 60% / 40%、塗りの 20%）を引き継いだ。

### 5.5 透過で色を作らない【事実】

`` `${c.voice}55` `` のように透過を足して色を作らない（ESLint で落ちる）。背面が分からないと測れない。
例外は下を隠してはいけない 3 つの重ね（`overlayScrim`、`selectionOverlay`、`recordingOverlay`）だけで、
意味は不透明な輪郭（`accentBorder` / `recSolid`）が運ぶ。テストが透過トークンをこの 3 つに限る。
無効状態も不透明度ではなく `textDisabled` / `surfaceRaised` / `border` の不透明な色で表す。

## 6. 空間・部品・動き【事実】

| 項目 | 規定 | トークン |
|---|---|---|
| 基本単位 | 4（`hair` 2 は例外） | `space` |
| 左右余白 | 20。幅 360 未満は 16 | `gutter` / `gutterCompact` / `useGutter()` |
| グループ | 内 8–12、外 24–32。外は内の 2 倍以上 | `space` |
| 行 | 最低 56（タップ領域は 48 以上） | `Row` |
| 通常ボタン | 最低 52 高、角丸 12、左右 16、ラベルは折り返す | `hit.button`、`Button` |
| 収録の操作 | 録音・停止 72、一時停止・再開 56 | `hit.record` / `hit.secondary`、`Transport` |
| タップ領域 | すべて 48 以上（WCAG 2.5.8 の 24 とは別の製品基準） | `hit.min`、`IconButton`、`hitSlop()` |
| カード | 角丸 16、内側 20。一覧をすべて囲わない | `Card` |
| シート | 上辺 24、閉じるボタン、下端 safe area | `Sheet` |
| 線 | 区切り 1、選択 2、焦点 3 | `stroke` |
| 押下 | 120ms 以内、0.96 の縮小。動きを減らす設定では縮小しない | `motion`、`pressScale` |

- 入れ子の角丸は外側 = 内側 + 余白（`concentric()`）。
- アイコンは 24 基準・線幅 2 の単一セット（`src/ui/Icon.tsx`、react-native-svg）。`▶ ⚙ ⋮ ＋` などの
  フォント字形をアイコンに使わない。主要操作には文字も付ける。
- 画面（`src/app/`、`src/features/`）と `src/ui/` は生の色・`fontSize`・余白・角丸を書かない（ESLint）。

### 6.1 共通部品【事実】

| 部品 | 要点 |
|---|---|
| `Screen` | safe area（上端は Home だけ `edgeTop`。ほかはナビゲーションバーが取る）、キーボード分のスクロール余白（iOS は `automaticallyAdjustKeyboardInsets`）、下部固定の操作バー（高さを実測して通知の位置に使う） |
| `ScreenHeader` | ネイティブのナビゲーションバーに題（と 2 行目の補足）を出す。`lockBack` の間は戻る操作を止め、理由をトーストで出す |
| `SectionHeader` | 見出しの役割を読み上げに伝える。上に分類ラベルを重ねない |
| `Button` | primary / secondary / danger / ghost。無効は不透明な色。`busy` で重複操作を防ぐ |
| `IconButton` | 48 の的。`accessibilityLabel` 必須。ラベル付き表示あり |
| `Row` | ラベル・補足・右側・下段（選択肢）。押せる行は読み上げに補足も含める。右側の操作は行の押下の外に置く（入れ子にしない） |
| `MoreMenu` / `HeaderMenu` | 「…」の操作。iOS はネイティブのメニュー、ほかはシート |
| `ChoiceMenu` | 値を 1 つ選ぶ行。iOS はメニュー形式のピッカー、ほかはシート（選択中にチェック） |
| `DateField` | 日付。iOS はコンパクトな日付ピッカー、ほかは `YYYY-MM-DD` の文字入力 |
| `Sheet` | iOS はページシート（下スワイプで閉じる）、ほかは下からのシート |
| `alerts` | `confirmDestructive` / `ask` / `notify`（OS のアラート）、`iosActionSheet`、`iosPrompt` |
| `Field` | 常に見えるラベル、補足、エラー（読み上げに関連付け）、焦点の輪郭 |
| `Segmented` | タブ（iOS は `UISegmentedControl`）。選択は一段明るい面 + 輪郭 1 周 + 文字色。片側の線は引かない（§2.1）。無効時は `textDisabled` |
| `Chip` | 高さ 40 + 上下の hitSlop で 48。選択は輪郭 2 と淡い地 |
| `Toggle` | OS の `Switch`。オンは `accentSolid`（iOS のつまみは OS の白のまま） |
| `Toast` | 操作バーの上。**閉じるボタンを持つ**。操作を含む通知・割り込み・容量不足は時間で消さない |
| `Notice` | 画面内の状態表示（注意・エラー・完了）。アイコンと文字を併せる |
| `ProgressBar` | 実値があれば確定、無ければ不定表示（架空の % を出さない） |

### 6.2 OS の標準部品を使う範囲（#98）【事実】

**作るのは PodsNow の個性が出る所だけ。** OS が持っている部品は使い、色・書体・太さ・角丸・配置だけを当てる。
自作の部品は、見た目が似ていても下スワイプで閉じる・長押しメニュー・戻るジェスチャ・読み上げの作法・触覚などの
振る舞いが欠ける（#94 の実装で起きていた）。

| 役割 | 使うもの | 当てるデザイン | iOS 以外 |
|---|---|---|---|
| ナビゲーションバー（Home 以外） | expo-router のネイティブスタックヘッダー | 題の書体、`accentText` の色合い、背景 `bg`、区切り線なし、戻るは矢印だけ | 同じ（Android は Material のツールバー） |
| 「…」の操作 | `Stack.Toolbar.Menu`（ヘッダー）、`@expo/ui` の `Menu`（行） | SF Symbols、削除は destructive | 現行のシート（`MoreMenu`） |
| 選択肢（設定の値など） | `@expo/ui` の `Menu` によるプルダウン（選択中にチェック） | — | 現行のシート（`ChoiceMenu`） |
| 確認・エラー・マイク権限・無音を詰める確認・名前の変更 | `Alert.alert` / `Alert.prompt`、言い直すは `ActionSheetIOS` | — | `Alert.alert`（Web は confirm）。名前の変更と言い直すはシート |
| 入力や一覧を伴うシート | `Modal` の `pageSheet`（下スワイプで閉じる） | 題の書体、閉じるボタン | 現行の下からのシート |
| スイッチ | `Switch` | オン `accentSolid` | 同じ |
| エピソードのタブ | `UISegmentedControl` | 書体、選択面 `surfaceRaised` | 現行の `Segmented` |
| アイコン | SF Symbols（`expo-symbols`） | 太さ medium、色はトークン | 現行の SVG（線幅 2） |
| 収録日 | `@react-native-community/datetimepicker`（compact） | `accentText` の色合い | 文字入力（`YYYY-MM-DD`） |
| 触覚 | `expo-haptics`（`services.haptics`、設定でオフにできる） | 録音の開始・停止は impact、タブや選択は selection、完了は success | Android も同じ API |
| 押下・通知の動き | Reanimated（UI スレッド）と Gesture Handler | `motion` の時間、`pressScale`。動きを減らす設定では動かさない | 同じ |

独自のまま残すもの: ロゴ、色、書体、Home 上部のブランド領域、録音の操作バー（`Transport`）、波形、
レベルメーター、`Card` / `Row` / `Button` / `Notice`（OS に同等の部品が無く、ブランドの形そのもの）。

- iOS だけの実装は `*.ios.tsx` に分け、Android と Web は既存の実装を使う。Android / Web で `@expo/ui` の
  SwiftUI 部品を読み込まないため。
- 収録中は戻るジェスチャと戻るボタンを止める（`gestureEnabled: false`、`usePreventRemove`）。
- 取り消せない操作（音声の削除）と、話数の再利用を伴う削除（書き出し済みの回）は OS のアラートで確かめる。
  iOS のメニューは項目の補足を出さないため、補足に書いていた注意をアラートへ移した。

## 7. 情報構造とナビゲーション【事実】

覚える階層は **番組 → エピソード → 工程**。工程は画面ではなく **録音 / 編集 / 書き出し の 3 タブ**（#93）。

| 場所 | 見せる操作 | 見せないもの |
|---|---|---|
| Home | 番組名、制作中の回（1 件）、一覧、下部の「新しいエピソードを録る」、番組と素材、復元、設定 | リスナー数、成長率、下部タブバー |
| エピソード（録音） | 状態、時間、レベル、入力、話すこと、ジングル・効果音、録音の一覧、言い直す・一時停止・収録を終える | 全編集ツール |
| エピソード（編集） | 再生位置、Undo / Redo、波形、選択と数値入力、カット、録り直す、素材、無音を詰める | 大きな販促コピー |
| エピソード（書き出し） | 通して聴く、音の仕上げ、詳細、形式、書き出す、履歴 | 実際には連携しないサービス |
| 配信の準備 | ファイル共有、タイトル・概要・全メタデータのコピー | 「公開済み」の表示 |

- Home → 「新しいエピソードを録る」→ 録音タブの「録音を開始」で **2 タップ**。タイトル入力を前置きしない。
- 収録中（録音・一時停止・割り込み・準備・保存中）はタブ移動と戻るを抑止し、「収録を終えてから移動して
  ください」を出す。OS のホームへ移る・画面をロックする行為は止めない。

## 8. 画面仕様【事実】

受け渡しの 9 画面を、現在の IA へ次のように対応付けた（理由は `docs/design-refresh/README.md` §3）。

| 設計の画面 | 実装 |
|---|---|
| 01 エピソード（Home） | `src/app/index.tsx` |
| 02 収録 | 録音タブ（`RecordTab` + `Transport`）。「マーカーを打つ」は #93 で廃止済みのため「言い直す」 |
| 03 編集 | 編集タブ（`EditTab` + `Waveform` + `Transport`）。収録/編集の 2 モードではなく 3 タブ |
| 04 音の仕上げ / 05 詳細 / 06 書き出し | 書き出しタブのセクション（`ExportTab`）。画面を分けない（#93） |
| 07 配信の準備 | `src/app/episode/[id]/share.tsx` |
| 08 番組と素材 | `src/app/show/index.tsx` + `AssetsSection` |
| 09 設定 | `src/app/settings.tsx` |
| バックアップ / 復元 | `src/app/episode/[id]/backup.tsx`、`src/app/restore.tsx` |

- **Home**: 上部に小さい `PodsNow.`、主見出しは番組名（上に分類ラベルを置かない）。制作中の回は 1 件だけ
  面で強調し、タイトル・EP 番号・尺・状態・「編集を続ける」などの動詞 1 つ。過去の回は行（等幅の話数、
  2 行までのタイトル、尺・状態）。初回は「エピソードはまだありません」と、タイトルは後から入力できること。復元した録音はボタンを覆わず、レイアウト内の通知で出す。
  音声を削除した回は「音声なし」と出し、0 秒と区別する。
- **録音タブ**: 上から「状態 → 時間 → レベル → 入力 → 話すこと → ジングル・効果音 → 録音の一覧」。
  下部に言い直す・一時停止/再開・録音/収録を終える を**位置を動かさずに**置く。空き容量は概算と明記し、
  取れなければ「確認できません」。「端末に保存中」は書き込みが続いているときだけ（エラー・容量不足で置き換える）。
  マイク未許可は説明シート → OS の許可、拒否後は「設定を開く」。Bluetooth 入力には音質の注意を近くに出す。
- **編集タブ**: 再生位置と全体、Undo / Redo を常設。波形は面の上に声（無彩色）と素材（藤色・青、名前付き）。
  選択は塊のタップ・ハンドル・**開始/終了の秒数入力**（読み上げでも操作できる）。選択時だけカット・試聴・
  録り直す・前後に素材。下部は 5 秒戻る / 再生 / 5 秒進む。
- **書き出しタブ**: 「声の音量をそろえる」「声に合わせて BGM を下げる」の 2 項目を先に。BGM が無ければ理由と
  「編集で BGM を入れる」。LUFS / dBTP は結果の説明として出し、詳細設定は折りたたむ。詳細はラベルを
  常に見せ、話数の重複はインライン警告（保存は止めない）。形式はラジオ。進捗は実ジョブの段階
  （「音を整えています」「ファイルを作成しています」）と値。キャンセルしても元音声と編集は消えないと明記。
  失敗は「音声の書き出しに失敗しました」「収録データと編集内容は端末に残っています」と再試行。
- **配信の準備**: 実ファイルができてから開く。冒頭に完了アイコンや見出しを積まず、ファイル（名前・尺・
  サイズ）と共有を最初に置く。ファイル共有が主操作、「まだ公開されていません」を常に出す。
  ファイルが消えていれば共有を無効にし「音声ファイルが見つかりません」。古い書き出しを開いたら日時と形式。
  コピーは書き込みが成功してから「コピー済み」。

## 9. 状態と失敗時の設計【事実】

| 状態 | 表示 | 主操作 |
|---|---|---|
| 読込中 | 「エピソードを読み込んでいます」 | — |
| 初回 | 「エピソードはまだありません」 | 新しいエピソードを録る |
| マイク未許可 / 拒否 | 用途の説明 / 収録できない理由 | 許可する / 設定を開く |
| preparing | 「マイクを準備しています」（収録中を先に出さない） | —（ボタンは busy） |
| recording | 丸 +「収録中」、時間、レベル、入力 | 収録を終える（四角） |
| paused | 2 本線 +「一時停止中」、止まった時間 | 再開（三角） |
| interrupted | 注意三角 +「割り込みで止まっています」。時間は加算しない | 再開 / 収録を終える |
| route change | 新しい入力名（通知と入力行） | — |
| storage low | 「空き容量が少ないため録音を停止しました」（時間で消さない） | 収録を終える |
| stopping | 「録音を保存しています」。重複停止を無効化 | — |
| recoverable crash | Home の通知「途中の録音を復元しました（尺）」 | 録音を確認する |
| audio purged | 「音声なし」 | — |
| export failed | 原因と「収録データと編集内容は端末に残っています」 | もう一度書き出す |
| copied | 「コピー済み」（書き込み成功後） | 再コピー |

通知は停止ボタン・主操作と重ならない（操作バーの上に出す）。操作を含む通知・割り込み・容量不足は時間で
消さず、閉じるボタンと読み上げラベルを持つ。「取り消す」は、その通知を出した操作がまだ履歴の先頭に
あるときだけ働き、その後に別の編集をしたら通知を閉じる（`EditingService.undoTopId`）。

## 10. アクセシビリティと多言語

### 10.1 実装で守っていること【事実】

- 全操作に名前・役割・状態（`accessibilityRole` / `accessibilityState`）。アイコンだけの操作は必ずラベル付き。
- 状態を色だけで示さない（録音 = 丸、停止 = 四角、一時停止 = 2 本線、割り込み = 注意三角 + 文字）。
- タップ領域 48 以上。隣の hitSlop と重ねない。
- 文字拡大を抑止しない。ボタン・行・シートは内容で伸びる。
- 時間は `accessibilityLabel` に持たせ、ライブリージョンにしない（毎秒読み上げない）。状態の変化はライブリージョン。
- 選択範囲は数値でも指定できる。範囲外はフィールドの下にエラーを出し、読み上げのヒントに関連付ける。
- OS の「動きを減らす」を尊重（押下の縮小、通知・不定進捗の動きを止める）。
- 文言は `src/i18n/ja.ts`（キーの正）と `en.ts` に置く。語順は関数で持つ。ユーザー入力は翻訳・置換しない。
- RTL は MVP 対象外（`supportsRTL: false`）。

【確認済み】48 は製品独自のタッチ目標。WCAG 2.2 AA の Target Size (Minimum) は例外付きの 24×24 CSS px で、混同しない。
出典: https://www.w3.org/TR/WCAG22/#target-size-minimum

### 10.2 実機で確かめること【仮説・未検証】

`docs/device-checklist.md` の K-1〜K-6、DS-1〜DS-11。Dynamic Type 最大 / Android 200%、VoiceOver / TalkBack、
アイコンのマスク、書体の実ウェイト、屋外での視認性は、Web 描画では確かめられない。

## 11. セキュリティ・プライバシー上の表示【事実】

- 書体・アイコンは同梱。起動時に外部へ通信しない。解析 SDK を入れない。
- 共有はユーザーが出力先を選ぶ操作。自動アップロード・クリップボード読取を追加しない（書き込みのみ）。
- 「ローカル保存」を「暗号化済み」「端末外へ出ない」と言い換えない（OS バックアップと共有先がある）。
- マイクが有効な間の状態を隠さない。

## 12. 置き場所と変更の手順【事実】

```
scripts/design/ramps.py        色の定義（唯一の正）
scripts/design/color.py        OKLCh と WCAG コントラストの計算
scripts/design/generate.py     → src/ui/tokens/colors.ts（検証してから書く）
scripts/fonts/generate.py      → assets/fonts/*.ttf（静的ウェイトの切り出し。fontTools）
scripts/brand/geometry.py      ロゴとアイコンの配置（唯一の正）
scripts/brand/glyphs.py        Manrope 800 の輪郭（extract_glyphs.py の生成物）
scripts/brand/generate.py      → assets/brand/*.svg、assets/images/*.png、src/ui/brand/wordmark.ts
src/ui/tokens/                 色（生成物）・分類（tones）・寸法と書体（scale）
src/ui/components.tsx          共通部品
src/ui/Text.tsx / fonts.ts     ロケール別の書体と退避
src/ui/Icon.tsx / Wordmark.tsx アイコンセットとロゴ
```

```sh
python3 scripts/design/generate.py    # 色。検証に落ちたら何も書かない
python3 scripts/brand/generate.py     # ロゴ・アイコン・スプラッシュ
python3 scripts/fonts/generate.py     # 書体（書体を替えるときだけ）
npm test                              # tokens / fonts のテストが二重に測る
npx expo prebuild --clean             # アイコン・書体を変えたらネイティブを作り直す
```

## 13. 未対応【事実】

- **実機での確認**。§10.2 の項目はすべて未検証。
- **Noto Sans JP の容量**（約 23 MB）。縮める案は `docs/design-refresh/README.md` §7。
- **ストレージの整理**（設定の「書き出し済みエピソードの元データを整理」）は従来どおり案内のみ。削除は
  Home のメニューの「音声を削除」「エピソードを削除」で行う。
- **カバーアート**は従来どおり未対応の表示。
