# DESIGN_SYSTEM.md — PodsNow のデザイントークン

画面の見た目を決める値を、どこに・どういう名前で・どうやって決めて置くか。

このファイルは **決めごとと理由**。値そのものは `src/ui/tokens/` にあり、色は
`scripts/design/ramps.py` から生成する。矛盾したら、コードではなくここを先に直す提案をする
（AGENTS.md）。

## 1. 置き場所

```
scripts/design/ramps.py        色の定義（唯一の正）。明度・色相・目標コントラスト比
scripts/design/color.py        OKLCh と WCAG コントラストの計算（標準ライブラリのみ）
scripts/design/generate.py     ↑ から src/ui/tokens/colors.ts を生成し、その場で検証する
src/ui/tokens/colors.ts        生成物。直接編集しない
src/ui/tokens/tones.ts         分類色のひとまとまり（文字・輪郭・地・塗り）
src/ui/tokens/scale.ts         余白・角丸・書体・触れる面・動き
src/ui/tokens/index.ts         入口。画面と ui/ はここからだけ読む
src/ui/contrast.ts             テストが比を測るための純粋関数
```

画面（`src/app/` / `src/features/`）と `src/ui/` は **生の値を書かない**。
`'#E2B979'`、`fontSize: 13`、`padding: 10`、`borderRadius: 14` はどれも ESLint
（`no-restricted-syntax`）で落ちる。

理由は歴史のほう。0.1.0 の途中まで生の値を許していて、文字サイズが 14 種類、余白が
14 種類、角丸が 16 種類まで増えていた。どれも設計ではなく、その場の目分量だった。

色を変えるときの手順:

```sh
# scripts/design/ramps.py を直してから
python3 scripts/design/generate.py   # 検証に落ちたら何も書き出さない
npm test                              # src/ui/__tests__/tokens.test.ts が二重に測る
```

## 2. 色

### 2.1 段には役割がある

グラデーションから目で選ぶのではなく、役割ごとに段を作る。役割の無い段は作らない。

| 役割 | トークン |
|---|---|
| 面 | `bg` / `surface` / `surfaceRaised` / `surfaceHover` / `overlayScrim` |
| 境界 | `border`（区切り）/ `borderStrong`（操作部品の輪郭。3:1 以上） |
| 文字 | `textPrimary` / `textSecondary` / `textTertiary` / `textDisabled` |
| アクセント | `accentSolid` / `accentSolidPressed` / `accentOnSolid` / `accentBorder` / `accentSubtle` / `accentText` |
| 破壊的操作 | `danger*`（同じ 6 つ） |
| 録音中 | `recSolid` |
| 分類 | `voice` / `music` / `insert` / `mistake` が `*Solid` `*Subtle` `*Fill` `*FillAlt` `*Text` `*Border` |

`border` と `borderStrong` を分けているのは、区切り線と操作部品の輪郭が別の要件だから
（後者には WCAG 1.4.11 の 3:1 がかかる）。いまは似た値でも、入力欄の見た目を変えた瞬間に分かれる。

### 2.2 値は計算する。目で決めない

`ramps.py` は OKLCh で色を作る。段には二種類ある。

- **面と塗り**は明度を決め打ちする。読みやすさではなく見た目の決めごとだから。
- **文字と境界**は目標コントラスト比から明度を**逆算**する。面を動かせば勝手に追従し、
  読めない組み合わせが残らない。

彩度は色相ごとの sRGB 上限に対する割合で持ち、ランプの中ほどで最大になる曲線を
全色相で共有する。同じ彩度の数値を色相間で使い回すと、上限が色相ごとに違うせいで
鮮やかさが揃わない。この曲線の係数は、ブランドの金 `#E2B979` をそのまま再現する値。

### 2.3 ひとつの色相にひとつの意味

色相どうしは 40° 以上離す（15° 以内は同じ色に見える）。0.1.0 の途中までは
`insert` が 19°（`danger` の 23° と 4° 差）、`mistake` が 64°（`accent` の 78° と 14° 差）にいて、
エディタ上で差し込みが REC ドットと、言い間違いの旗がアクセントと見分けられなかった。
いまは `insert` が青 240°、`mistake` が薔薇 340°。

ひとつだけ意図的に色を共有している。**声トラック（`voice`）と「録れている / 済んでいる」
（`success`）は同じ色**。このアプリでは「声が入っている」がそのまま完了を意味するので、
別の色にすると同じことを二色で言うことになる。

### 2.4 検証を二重に置く

`generate.py` は書き出す前に 160 組のコントラストを測り、ひとつでも落ちたら何も書かない。
`src/ui/__tests__/tokens.test.ts` は生成物に対して同じ組み合わせを測り直す。

二重なのは、生成を忘れて `colors.ts` を手で書き換えたときに CI で落とすため。実際、
チップの輪郭が自分の淡い地に対して 2.5:1 しかなかった見落としは、テスト側が先に見つけた。

基準【確認済み】:

| 対象 | 比 | 出典 |
|---|---|---|
| 本文・ラベル | 4.5:1 | https://www.w3.org/TR/WCAG22/#contrast-minimum |
| 操作部品の輪郭・状態を示す塗り | 3:1 | https://www.w3.org/TR/WCAG22/#non-text-contrast |
| 無効状態 | 3:1（要件外。見えなくはしない） | — |

### 2.5 透過で色を作らない

`` `${c.voice}55` `` のような組み立てをしない。背面が分からないとコントラストを測れず、
測れないものは検証できない。波形の塗りのような淡い面は、最初から不透明なトークン
（`*Fill` / `*Subtle`）として生成する。

例外は三つだけで、どれも「下を隠してはいけない重ね」。`overlayScrim`（モーダルの背面）、
`selectionOverlay` と `recordingOverlay`（波形に重ねる帯）。帯を不透明にすると、
どこを選んでいるかより先に何が録れているかが読めなくなる。意味そのものは不透明な輪郭
（`accentBorder` / `recSolid`）が運ぶので、帯が薄くても情報は落ちない。
透過を持つトークンがこの三つだけであることはテストで確認している。

### 2.6 ブランドとの関係

アイコンとスプラッシュの色は `scripts/brand/geometry.py` がトークンの生成元
（`ramps.py`）を直接読む。マークのために別の色を作らない、という
`assets/brand/README.md` の決めごとを、コメントではなく import で守る。

トークンの dark を変えたら `python3 scripts/brand/generate.py` と `app.json` の
`backgroundColor` も合わせる。

## 3. 余白

`space`（4 の倍数）と `gutter`（画面の左右余白 = `space.lg`）。

**グループの外側は内側の 2 倍以上あける。** 内側が `sm`(8) なら外側は `lg`(16) 以上。
ここが崩れると、まとまりが線ではなく騒がしさに見える。区切りは
「余白 → 面の色 → 線」の順で試し、線は余白で表せないときだけ使う。

## 4. 角丸

`radius`（`xs` 4 / `sm` 8 / `md` 12 / `lg` 20 / `xl` 24 / `pill`）。

入れ子は **外側 = 内側 + 余白**。`concentric(outer, pad)` で計算し、目分量で合わせない。
カード（`radius.lg` 20、余白 `space.lg` 16）の内側に置く要素は `radius.xs` 4 になる。

## 5. 書体

`typography` は役割ごとに **大きさ・行間・太さをひとまとめ**にしている。役割をひとつ
選べば三つ決まる。強調は大きさではなく太さを一段上げる。

| 役割 | 用途 |
|---|---|
| `display` | 録音時間、Show 名 |
| `title` | 画面タイトル |
| `heading` | セクション見出し |
| `body` / `bodyStrong` | 本文、行の一覧 |
| `label` | ボタン・チップ |
| `caption` | 補足、空状態 |
| `overline` | 小見出し、バッジ |

守っていること（テストで確認する）:

- 11px を下回らない。
- 18px 未満で太さ 300 以下は使わない。細い字は本文サイズだと消える。
- 折り返しうる役割の行間は 1.4 倍以上。
- 見出しは役割が下がるほど小さくなる。

変わり続ける数値（タイムコード、カウンタ）には `tabularNums` を付ける。等幅数字に
しないと、桁が変わるたびに右の要素が揺れる。

字形アイコン（`⋮` `▶` `＋`）は `icon` で、書体とは別の尺度。行の中の文字ではなく
押せる的だから。

## 6. 触れる面

`hit.min` = 44（Apple HIG / WCAG 2.5.5）、`hit.compact` = 36（間隔が取れている密な場所）。
WCAG 2.5.8 (AA) の下限は 24 だが、片手で持って録音するアプリなので 44 を基準にする。

見た目が小さくてよくても、触れる面は広げる。文字ひとつを押させるところは `glyphSlop`。
個別に 6 / 8 / 10 と散らすと、どれも 44 に届かないまま揃いもしない。

## 7. 動き

`motion`（`instant` 120 / `quick` 160 / `moderate` 240）と `pressScale` 0.96。

- 頻繁に起きる操作の反応は 150ms 以下。
- **動きだけで状態を伝えない。** 色・文字・アイコンのどれかを必ず併せる。
- OS の「動きを減らす」設定を尊重する（`useReducedMotion()`）。移動と拡大をやめ、
  不透明度の切り替えだけにする。
- 押下は不透明度ではなく**押下用の色の段**（`*SolidPressed`）で表す。不透明度で暗くすると、
  背面しだいでラベルとのコントラストが変わり、測った値が当てにならなくなる。

## 8. 未対応

いまのトークンでは解けていないもの。

- **アクション付きトーストが時間で消える**（`useToast`）。WCAG 2.2.1 は、時間制限を
  延長・解除できることを求めている。取り消しの導線が勝手に消えるのはこれに触れる。
  PRODUCT.md §6 の「削除は確認なし、直後に取り消す」を変える話になるので、別 Issue。
- **RTL**。`supportsRTL: false`（app.json）なので、いまは物理方向のままで齟齬がない。
  対応するときは `*Start` / `*End` へ寄せる。
- **実機での確認**。値と比は計算で確かめてあるが、実機の画面で見た確認はしていない。
  `docs/device-checklist.md` の項目としてまだ消化していない。
