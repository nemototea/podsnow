# 実機チェックリスト（Issue #51）

自動テストで検証できない項目。各項目は **iOS / Android の両方**で、結果（日付・端末・OS・合否・メモ）を下の表に残す。
AUDIO_DESIGN.md §11 の S-1〜S-10 に対応。

## 前提

- Development Build（`npm run ios` / `npm run android`）。iOS は Xcode 26 以上が必要（Expo SDK 57）。
- Mac から離れて確認する項目（B / C / D / E / F など）は **release variant** を入れて行う
  （DEVELOPMENT.md §3.1、`npx expo run:android --variant release --device <model>`）。Metro が不要になる。
- `expo-localization` の追加とその config plugin（`CFBundleLocalizations` / `locales_config.xml`）を反映するため、**J. の確認前に `npx expo prebuild --clean` をやり直す**（Issue #80）。
- マイク権限・通知権限（Android 13+）を許可済み。
- 空き容量 1 GB 以上。

## A. 録音の基本

| # | 手順 | 期待 |
|---|---|---|
| A-1 | Home → ＋ → 録音ボタン → 30 秒話す → 停止 | Take が声トラック末尾に追加され、波形が出る。テイク名「録音 1」 |
| A-2 | 録音中に一時停止 → 10 秒待つ → 再開 → 停止 | 一時停止中の時間は Take に含まれない。ファイルは 1 つ（seg-0001.wav） |
| A-3 | 録音中に「● マーカー」「噛んだ」を押す | 停止後、波形下にマーカーが出て、バナー「マーカー n 件」→「次へ」で移動できる |
| A-4 | Show Assets にジングルを登録し ★ にしてから録音中に「♪ <名前>」を押す | 停止後、素材レイヤーに挿入され、その発言位置に追従している |

## B. バックグラウンド（S-1 / S-2）

| # | 手順 | 期待 |
|---|---|---|
| B-1 | 録音開始 → 画面ロック → **30 分**放置 → 解除 → 停止 | 30 分分の Take。途中で途切れていない（波形が連続、Segment 1 つ） |
| B-2 | 録音開始 → ホームへ戻り他アプリ（メモ / ブラウザ）を 10 分使う → 戻って停止 | 同上。Android は通知「PodsNow — 収録中」（英語端末では「PodsNow — Recording」）が出ている |
| B-3 | Android: 録音中に通知をタップ | アプリが前面に戻る |

## C. 割り込み（S-1 / S-2）

| # | 手順 | 期待 |
|---|---|---|
| C-1 | 録音中に**電話を着信**（別の端末からかける）→ 出ずに切る | 「割り込みで録音が止まりました」→ 録音ボタンで再開 → seg-0002.wav が作られ、`⏸` マーカーが打たれる。着信前の音声は残っている |
| C-2 | C-1 で設定「割り込み後に自動で再開」を ON | 通話終了後に自動で再開する（iOS: `shouldResume` が付く場合のみ） |
| C-3 | 録音中に他アプリで音楽を再生する | 割り込みとして扱われる（または継続する）。どちらでも録音データが失われない |
| C-4 | 録音中に Siri / アシスタントを起動 | 同上 |

## D. 強制終了と復旧（S-3）

| # | 手順 | 期待 |
|---|---|---|
| D-1 | 録音開始 → 2 分話す → アプリスイッチャーからスワイプで強制終了 → 起動 | トースト「未確定の録音を復元しました（約 02:00）」。Take は `recovered`、末尾 1 秒以内まで再生できる |
| D-2 | 録音中に端末を再起動 | 同上（Android は FGS も落ちる） |
| D-3 | D-1 の Take で「無音を削除」「範囲を削除」 | 通常の Take と同じに編集できる |

## E. 入力デバイス（S-5）

| # | 手順 | 期待 |
|---|---|---|
| E-1 | 有線マイク（USB-C / Lightning）を挿して設定 → 録音 | 設定の入力一覧に出る。録音 Take の `input_label` がそのデバイス名 |
| E-2 | 録音中に有線マイクを抜く | 録音は継続、トースト「入力が 内蔵マイク に切り替わりました」、`route_change` マーカー |
| E-3 | Bluetooth ヘッドセットを接続し入力に選ぶ | 設定に「音質が落ちます」の警告。録音できる |
| E-4 | オーディオインターフェースをステレオ入力で | 設定「ステレオ」で 2ch の WAV |

## F. ジングルのモニター（S-4）

| # | 手順 | 期待 |
|---|---|---|
| F-1 | イヤホン接続 + 設定「イヤホン接続時のみ」で録音中にジングルを押す | イヤホンから鳴る。録音は止まらない。**録音にはジングルが入っていない** |
| F-2 | スピーカー出力で同じ操作 | 鳴らない。トーストに「モニター再生なし」 |
| F-3 | 設定「常に」+ スピーカー | 鳴る。録音に回り込む（仕様通り） |

## G. ストレージ（S-10）

| # | 手順 | 期待 |
|---|---|---|
| G-1 | 設定「想定録音時間」を 120 分にし、空き容量が足りない端末で録音開始 | 「空き容量が不足しています（必要 n MB / 空き m MB）」で開始しない |
| G-2 | 空き容量を 50 MB 程度まで埋めて録音 | 残り 30 MB で自動停止、Take は確定、トースト表示 |

## H. 書き出し（S-6 / S-7）

| # | 手順 | 期待 |
|---|---|---|
| H-1 | 60 分のエピソードを M4A（Podcast）で書き出し | 3 分以内。進捗が進む。書き出し履歴に LUFS / dBTP が出る |
| H-2 | H-1 のファイルをラウドネスメーター（例: `ffmpeg -af ebur128` / Youlean）で測る | 統合ラウドネス -16 ± 1 LU、トゥルーピーク ≤ -1 dBTP |
| H-3 | WAV で書き出し → PC で開く | 48 kHz / 16 bit、長さが一致 |
| H-4 | BGM 付きで書き出し | しゃべり中に BGM が下がり、無音で戻る |
| H-5 | iOS / Android で同じバックアップから同じプリセットで書き出し | 聴感で同じ。可能なら波形差分 |

## I. 素材取り込み

| # | 手順 | 期待 |
|---|---|---|
| I-1 | MP3 / M4A / WAV（44.1 kHz ステレオ）をそれぞれ取り込み | 48 kHz モノラル WAV に変換され、試聴できる。ピッチが変わっていない |

## J. ローカライゼーション（Issue #80）

言語の切り替えは実機でしか確かめられない項目がある。各項目を **iOS / Android の両方**で。

| # | 手順 | 期待 |
|---|---|---|
| J-1 | 端末の言語を日本語にしてアプリを起動 | UI が全面日本語。セクション見出し（設定の「言語」「表示」「収録」など）も日本語 |
| J-2 | 端末の言語を英語にして起動 | UI が全面英語。ステータスは Draft / Ready / Exported |
| J-3 | 端末の言語を未対応の言語（例: フランス語）にして起動 | 英語になる（FR-I18N-1 のフォールバック） |
| J-4 | 設定 → 言語 → 「日本語」/「English」を選ぶ | **再起動なしで**その場で全画面が切り替わる。アプリを再起動しても選択が残る |
| J-5 | 設定 → 言語 → 「システムに合わせる」に戻す | 端末の言語に従う |
| J-6 | アプリを消して、端末を英語にして初回インストール → 起動 | マイク権限ダイアログが英語。Show 名が "My Podcast"、最初のエピソードが "Episode 1"、Take が "Recording 1"（FR-I18N-6 / FR-I18N-7） |
| J-7 | J-6 の状態で日本語に切り替える | UI は日本語になるが、**すでに作られた Show 名・エピソードタイトルは英語のまま**（ユーザーのデータなので書き換えない。FR-I18N-6） |
| J-8 | Android: 英語端末で録音開始 → 通知シェードを見る | 通知が "PodsNow — Recording" / "Tap to return"。日本語端末では「PodsNow — 収録中」 |
| J-9 | Android 13+: 設定 → アプリ → PodsNow → 言語 | 「日本語」「English」が選べる（`locales_config.xml`、FR-I18N-8）。選ぶと UI が切り替わる |
| J-10 | 英語端末でランチャーとアプリ情報を見る | アプリ名が "PodsNow" |
| J-11 | 英語端末で書き出し → Distribution Pack の「ALL METADATA」をコピー | 見出しが Title / Episode / Season / Recorded / Duration / File |
| J-12 | 日本語で作ったバックアップ（.podsnow）を英語端末で復元 | 復元できる。進捗とエラーの文言が英語 |

## K. アイコン・ブランド（Issue #82 → Issue #94 で差し替え）

マイク + レベルメーターのマークを廃止し、ロゴタイプ `PodsNow.` と 2 段の文字アイコン `Pods` / `Now.` にした。
見え方は実機でしか確認できない。**確認前に `npx expo prebuild --clean` → 再ビルドが必要**（アイコンと書体はネイティブ側に焼き込まれる）。

| # | 手順 | 期待 |
|---|---|---|
| K-1 | ホーム画面のアイコンを 48 / 60 pt で見る（iOS / Android 両方） | シトロン地に `Pods` / `Now.` の全文。点が欠けない。マイク・波形が無い |
| K-2 | Android: 設定 → 壁紙とスタイル でアイコンの形を「円」「角丸四角」「しずく」に変えて見る | どの形でも `Pods` の P と `Now.` の点が欠けない（前景の最大描画半径 311px / 安全域 338px） |
| K-3 | Android 13+: テーマアイコンを ON にする | 単色の `Pods` / `Now.` が出る（`android-icon-monochrome.png`） |
| K-4 | アプリを起動してスプラッシュを見る | 背景 `#111416`、横組み `PodsNow.` が中央。点はシトロン |
| K-5 | iOS: 設定 → PodsNow / App Store のプレビュー | アイコンの角が黒く抜けていない（アルファ無しで書き出している） |
| K-6 | 設定の検索結果・通知など 32pt 以下の小さい表示 | 全文が残る（読みやすさの評価は別。頭文字に略されていない） |

## DS. デザイン刷新（Issue #94）

Web 描画（react-native-web、`docs/design-refresh/README.md` §6）では確かめられない項目。

| # | 手順 | 期待 |
|---|---|---|
| DS-1 | 日本語 UI と英語 UI でホーム・録音・編集を開き、見出しと本文を拡大して見る | 日本語は Noto Sans JP、英語は Manrope。太字が合成（にじみ・太り）ではなく同梱した実ウェイトで描かれる。時間は IBM Plex Mono |
| DS-2 | `assets/fonts/` を外した（または prebuild していない）ビルドで起動して録音する | 書体が OS 標準に退避するだけで、起動・録音・停止が止まらない |
| DS-3 | iOS の Dynamic Type を最大、Android の文字サイズを 200% にして全画面を回る | 文字が切れずに折り返す。停止・一時停止・書き出し・共有に到達できる。下部の操作バーが画面を覆い尽くさない |
| DS-4 | 幅 320pt 相当（iPhone SE / 小型 Android）で録音 → 編集 → 書き出し | 横スクロールが出ない（波形の時間軸を除く）。タイマーが 40pt に縮む |
| DS-5 | VoiceOver / TalkBack で録音タブを操作する | 状態（収録中 / 一時停止中 / 割り込み）、入力名、操作名（録音を開始・一時停止・収録を終える・言い直す）が読まれる。タイマーが毎秒読み上げられない |
| DS-6 | 収録中に「編集」「書き出し」タブや戻るを押す | 移動せず「収録を終えてから移動してください」が出る。OS のホームへ移るのは止めない |
| DS-7 | 初回、マイク権限が未決定の状態で「録音を開始」 | 説明シートが出てから OS の許可ダイアログ。拒否後は「設定を開く」 |
| DS-8 | 編集で声の塊をタップ | 塊が選択される（Issue #94 で、タップを奪っていた透明な Pressable を外した） |
| DS-9 | カット → 別の編集 → 最初の通知の「取り消す」 | 通知は別の編集の時点で閉じる。後の編集が意図せず取り消されない |
| DS-10 | ライトテーマで主操作ボタン・トグル・再生ヘッドを見る | シトロンの塗りに濃い輪郭があり、白地でも形が分かる |
| DS-11 | 屋外の明るい場所と暗い部屋で録音タブを見る | 細い境界・小さい数値・レベル計が判読できる |

## 結果

| 日付 | 端末 / OS | 項目 | 合否 | メモ |
|---|---|---|---|---|
| | | | | |

## 画面の作り直し（docs/ux-restructure.md）で追加した確認項目

いずれも実機でしか判断できない。未検証のまま完了と言わない（AGENTS.md）。

| # | 手順 | 期待 |
|---|---|---|
| UX-1 | 録音タブで「話す内容」を出したまま 30 分録音し、画面ロックと他アプリ切替をはさむ | 録音が欠けない。描画が録音を妨げない |
| UX-2 | 60 分の素材を編集タブで開き、選択のハンドルを端から端までドラッグする | 追従が途切れない（目標 60fps）。離した位置と確定位置がずれない |
| UX-3 | 割り込み（着信）で分割された録音に対して「言い直す」を使う | 捨てた範囲だけが声の並びから外れ、録音ファイルの長さは変わらない |
| UX-4 | 台本（本文つきの項目）を見ながら 10 分読み上げる | 文字サイズと行間で読める。スクロールが追いつく |
| UX-5 | 素材ボタン 4 件を片手で押す（大画面・小画面の両方） | 48pt の的を確保できている（Issue #94 で 44 → 48）。誤爆しない |
| UX-6 | 塊をタップして削除 → 取り消す | 見えている塊と消える範囲が一致する。取り消しで元に戻る |
| UX-7 | チャプターのラベルを長押し | その項目の範囲だけが選ばれる |
