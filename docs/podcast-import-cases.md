# 番組の取り込み — 準正常系・異常系の設計（Issue #101）

> 凡例: **【事実】** 決定済み・コードで確認済み / **【確認済み】** 出典あり / **【仮説】** 未検証 / **【提案】** 未決。ユーザー判断待ち
>
> 対象: `src/services/podcast/PodcastImportService.ts`、`src/app/import.tsx`、`feed_episodes`、話数の採番（REQUIREMENTS.md §2.1.1）。
> 配信基盤（Issue #107）との接続点も含める。

## 1. なぜこの文書が要るか

最初の実装（`claude/rss-import-feature-bknvuu`）は正常系を通すことに寄っていた。
「取り込む」は番組情報・配信済みの回・話数の台帳・アートワークを**まとめて書き換える**操作で、
しかも後で #107 の配信基盤から**配信する元**になる。準正常系・異常系を決めずに進めると、次が起きる。

- 別の番組の回が自分の番組に混ざり、話数の台帳が壊れる（§4 B-1。**今のコードで起きる**）
- 他人の番組を取り込んで、そのまま配信基盤から配信できてしまう（§4 A-2。#107 の実装時に起きる）
- 手で直した番組情報が、読み込み直しで黙って元に戻る（§4 C-5。**今のコードで起きる**）

## 2. 前提【事実】

- MVP は 1 Show 固定（FR-SHOW-1）。取り込みは「今の番組の中身を入れ替える」操作であって、番組を増やす操作ではない。
- 取り込みは端末の中だけで完結する。外に出るのは検索語と RSS の取得だけ（NFR-5）。
- RSS は公開情報。取り込めたことは所有者である証明にならない（FR-SHOW-11）。
- 配信済みの番号は再利用しない（§2.1.1）。台帳は `episodes` と `feed_episodes`。

## 3. 原則【提案】

| # | 原則 | 理由 |
|---|---|---|
| P1 | **取り込みは制限しない。配信の入口で所有権を確かめる。** 取り込みは Podcast アプリで番組を購読するのと同じ「公開情報の写し」で、端末の外に害は出ない。害が出るのは、他人の番組を自分の名前で配信するとき | 取り込みの段階で所有権を確かめようとしても、サーバーが無いので確かめる手段が無い。確かめられるのは #107 の配信基盤 |
| P2 | **書く前に「同じ番組か」を判定する。違う番組なら「差し替え」として扱い、混ぜない** | 配信済みの回と話数の台帳は番組ごとのもの。混ざると元に戻せない |
| P3 | **ユーザーが手で直した値を黙って上書きしない** | 読み込み直しは何度も行う操作（配信状況の更新）。毎回番組情報が戻るなら使えない |
| P4 | **失敗したら何も書かない**（DB もファイルも） | 途中で落ちたときに、半端な状態を残さない（NFR-1 と同じ考え方） |
| P5 | **外から来た値は、使う場所で安全かを確かめる**（URL のスキーム、HTML、長さ） | RSS は誰でも書ける（NFR-10） |

## 4. ケース

「現在」はこのブランチのコードを読んで確かめた挙動【事実】。「提案」は未決【提案】。
優先度: **高** = データが壊れる・他人に害が出る、**中** = 利用者が混乱する・直すのに手間がかかる、**低** = まれ・害が小さい。

### A. 誰の番組か（所有権）

| ID | 状況 | 現在 | 提案 | 優先度 |
|---|---|---|---|---|
| A-1 | 同じ名前の**別の番組**を、間違えて選んで取り込む | そのまま取り込める。プレビューで番組名・アートワーク・著者は見える | プレビューに「配信済みの回」の直近 3 件の題名と日付を出し、自分の番組かを見分けやすくする。取り込み後も元に戻せるようにする（§4 B-1 の差し替えと同じ仕組みで、直前の状態へ戻す） | 中 |
| A-2 | **他人の番組を意図的に**取り込む | 取り込める。端末の中だけなら、他人に害は出ない | 所有権の確認を**取り込みの流れの中**に置く（Spotify for Creators と同じ。§8）。確認は配信基盤（#107）のサーバーでしかできないので、配信基盤ができるまでは「未確認」として取り込み、未確認の番組は配信できないようにする | 高 |
| A-3 | 自分の番組だが、`podcast:locked` が yes | `shows.locked = 1` として保存するだけ | 取り込みは許す（端末に写すだけで、ホスティングの移転ではない）。#107 へ移るときに「今の配信サービスで locked を外す」手順を案内する【確認済み】(https://podcasting2.org/docs/podcast-namespace/tags/locked) | 中（#107） |
| A-4 | 他人の番組の概要・アートワーク・所有者のメールアドレスが端末に残る | 残る | 差し替え（B-1）と、番組情報を初期状態に戻す操作で消せるようにする。所有者のメールは画面に出さない（配信時の確認にだけ使う） | 低 |

### B. 別の番組・別の経路での取り込み（同一性）

| ID | 状況 | 現在 | 提案 | 優先度 |
|---|---|---|---|---|
| B-1 | 番組 A を取り込んだあと、**別の番組 B** を取り込む | **混ざる。** 番組情報は B で上書き、`feed_episodes` は A の回と B の回が並ぶ、Apple の番組 ID は B で上書き、カテゴリーは B で置換。**話数は A の最大値の続きのまま**（例: A が 120 回、B が 10 回なら、次は #121） | §5 の規則で「別の番組」と判定したら、取り込む前に「番組を差し替える」確認を出す。差し替えでは **A の配信済みの回・外部 ID・カテゴリー・支援リンク・アートワークを消してから** B を入れる。**手元で作ったエピソード（録音）は消さない**。確認文で「配信済みの回 120 本の記録が消え、話数は B の続きになる」と数字で伝える | **高** |
| B-2 | 同じ番組の RSS の URL が変わった（配信サービスの移転。`itunes:new-feed-url` や 301 で移っている） | 別の URL として取り込み、`feed_url` を新しい方に書き換える。回は `guid` が同じなら重ならない | §5 の規則で「同じ番組」と判定し、差し替えの確認を出さない。`feed_url` の変化はプレビューに「RSS の URL が変わりました（旧 → 新）」と出す | 中 |
| B-3 | 同じ番組を、検索経由と URL 直接入力の両方で取り込む | 同じ番組として扱える（`guid` で突き合わせ）。URL 直接入力では Apple の番組 ID を書き換えないので、前の値が残る | 同じ番組ならそれでよい。ただし B-1 の差し替えでは、前の番組の ID が残って食い違うので、差し替え時に消す | 低 |
| B-4 | 「新しく始める」で手元にエピソードを作ったあと（#1〜#3）、配信中の番組（120 回）を取り込む | 取り込める。次は #121。手元の #1〜#3 はそのまま残り、配信済みの #1〜#3 と話数が重なる | プレビューで「手元のエピソード 3 本はそのまま残る。配信済みの回と同じ話数のものがある」と伝える。手元の回を配信済みの回に結びつける（`feed_episodes.episode_id`）のは、話数・題名が一致する候補を出してユーザーが選ぶ形にする（自動では結ばない） | 中 |
| B-5 | PodsNow の配信基盤（#107）へ移ったあとで、**旧配信元の RSS** を読み込み直す | 旧 RSS の内容で上書きする（旧 RSS が 301 で新 RSS を指していれば、新 RSS を読む） | 自分の配信基盤が正本になった番組は、外部 RSS から読み込み直さない。配信状況は配信基盤から取る（FR-PUB-5）。`feed_url` が自分の配信基盤の URL なら、取り込み画面に「読み込み直す」を出さない | 中（#107） |

### C. 同じ番組の読み込み直し

| ID | 状況 | 現在 | 提案 | 優先度 |
|---|---|---|---|---|
| C-1 | RSS に最新 N 件しか載っていない | 載っていない回も残す【事実】 | そのまま | — |
| C-2 | 配信者が回を**本当に取り下げた** | C-1 と区別できず、残る | 各回に「最後に RSS で見た時刻」を持ち、読み込み直しで見えなくなった回は「RSS に無い」と表示する。消しはしない（話数の台帳から外さない。取り下げた番号を再利用すると、リスナーのアプリで混乱する） | 低 |
| C-3 | 配信サービスの移転で、回の `guid` が**振り直された**（本来は規格違反） | 同じ回が別の行として 2 本ずつになる | 読み込み直しで「`guid` が 1 つも重ならない」なら同一番組でも警告する。`enclosure` の URL、または題名と配信日時の一致で重複を検出し、プレビューに「同じ回が重複しそうです」と出す | 中 |
| C-4 | 配信者が話数を直した（#12 → #13） | 上書きする【事実】 | そのまま。手元で既に使った番号と重なる場合は B-4 と同じ扱い | 低 |
| C-5 | 手で直した番組名・概要が、読み込み直しで**上書きされる** | **上書きされる**（RSS に値があれば必ず） | 初回（または差し替え）は上書き。**読み込み直しでは番組情報を上書きせず、RSS 側と違う項目をプレビューに並べて、項目ごとに「RSS の値にする」を選べる**ようにする。回（`feed_episodes`）は常に更新 | **中** |
| C-6 | `guid` が無く、音声の URL で代わりにしていた回の音声 URL が変わる | 別の回として 2 本になる | C-3 と同じ重複検出の対象にする | 低 |

### D. 話数

| ID | 状況 | 現在 | 提案 | 優先度 |
|---|---|---|---|---|
| D-1 | B-1 で別の番組に差し替えた | A の最大値の続きになる | 差し替えで A の `feed_episodes` を消すので、B の続きになる | **高**（B-1 と同時） |
| D-2 | RSS の話数に打ち間違い（#12 のはずが #1200） | 次が #1201 になる | プレビューの「次の新しいエピソードは #N から」で気づける【事実】。加えて、話数が飛んでいる（直前の話数より 100 以上大きい等）ときはプレビューで注意を出す【仮説: しきい値】 | 低 |
| D-3 | シーズンごとに話数を 1 から振り直す番組 | 全シーズンの最大値の続きになる（シーズン 3 の #1 を作るつもりが #25） | MVP は全体の最大値で扱う（REQUIREMENTS.md FR-PUB 周りと同じ）。新規エピソードの季を既定のシーズンにしたとき、そのシーズンの最大値の続きにするかは別に決める【提案】 | 低 |

### E. 通信・入力・端末の異常

| ID | 状況 | 現在 | 提案 | 優先度 |
|---|---|---|---|---|
| E-1 | オフライン・タイムアウト・404・5xx・HTML・巨大・http・http へのリダイレクト | 理由つきのエラーを出し、何も書かない【事実】（テストあり） | そのまま | — |
| E-2 | 検索 API が混んでいる（403 / 429） | 「取得できませんでした（429）」と出る | 429 / 503 は「混み合っています。しばらくしてから」と出し分ける。RSS の URL 直接入力へ誘導する | 低 |
| E-3 | アートワークだけ失敗 | 取り込みは成功、「アートワークは保存できませんでした」と出す【事実】 | そのまま | — |
| E-4 | 取り込みの途中でアプリが終了する | DB はトランザクションで戻る。**アートワークのファイルは DB より先に書くので、孤立ファイルが残りうる**（`cover.png` が残り、DB は `cover.jpg` を指す等） | アートワークは一時ファイルに書き、DB の確定後に置き換える（P4） | 低 |
| E-5 | 「取り込む」の連打、取り込み中に戻る | 連打はボタンが busy になり防げる【事実】。戻ったあとも処理は続き、完了する | 取り込み中は戻る操作を止める（`ScreenHeader` の `lockBack`）。検索中・読み込み中の戻るは許す（書き込みが無いので） | 低 |
| E-6 | Shift_JIS など UTF-8 以外の RSS | `fetch` の `text()` が UTF-8 として読み、文字化けする可能性【仮説】 | XML 宣言の `encoding` を見て、UTF-8 以外なら「この RSS の文字コードには対応していません」と出す（黙って化けさせない）。対応は実例が見つかってから | 低 |
| E-7 | 数千回ある番組 | 1 トランザクションで全件を書く。時間は未計測【仮説】 | 実機で 3,000 回程度を計測してから判断。遅ければ進捗を出す | 低 |
| E-8 | RSS の URL 項目が `javascript:` などの危険なスキーム | `link` と各回の `link` / 画像 URL をそのまま保存する（アートワークの取得は https だけ） | **保存時に `https:` / `http:` 以外の URL を捨てる**（P5）。表示でリンクにするときも同じ判定を通す | **中** |
| E-9 | 各回の概要の HTML | そのまま保存（乗り換え先の RSS に載せるため）【事実】 | 画面に出すときは必ず文字にしてから出す（`htmlToPlainText`）。WebView で描画しない | 中（一覧を作るとき） |
| E-10 | 極端に長い番組名・概要・回の数 | 長さの上限なし | 保存前に上限を設ける（番組名 255 字、概要 4,000 バイト＝ PSP-1 の上限【確認済み】(https://github.com/Podcast-Standards-Project/PSP-1-Podcast-RSS-Specification)、回の数 10,000 等【仮説】） | 低 |

### F. プライバシー

| ID | 状況 | 現在 | 提案 | 優先度 |
|---|---|---|---|---|
| F-1 | 検索語（番組名）が Apple に送られる | 送られる（NFR-5 に明記）【事実】 | 検索欄の下に「番組名を Apple の検索に送ります」と小さく出す | 低 |
| F-2 | 取り込んだ所有者のメールアドレス | 保存する | A-4 と同じ。画面に出さない | 低 |

## 5. 同一番組の判定規則【提案】

取り込もうとしている番組（新）と、今の番組（旧）を、上から順に比べる。**最初に決まったもの**を採る。

| 順 | 比べるもの | 一致 | 不一致 |
|---|---|---|---|
| 1 | 旧に何も取り込んでいない（`feed_imported_at` が空） | 初回 → 上書き（確認なし） | — |
| 2 | `podcast:guid`（両方にあるとき） | 同じ番組 | 別の番組 |
| 3 | Apple の番組 ID（両方にあるとき） | 同じ番組 | 別の番組 |
| 4 | RSS の URL（旧の `feed_url` と、新の取得 URL・自己申告の URL・`itunes:new-feed-url` のどれか） | 同じ番組 | 次へ |
| 5 | 回の `guid` の重なり（旧の `feed_episodes` のうち、新の RSS に含まれる割合） | 半分以上が重なれば同じ番組【仮説: しきい値】 | 別の番組 |

- 「別の番組」→ 差し替えの確認（B-1）。確認なしには書かない。
- 「同じ番組」→ 読み込み直し（C-5 の差分表示）。
- 4 で決まらず 5 で「重なりが少ないが 0 ではない」ときは、同じ番組として扱い、C-3 の重複警告を出す。

## 6. 決めたこと【事実】ユーザー判断（2026-09-26）

| # | 論点 | 決定 |
|---|---|---|
| Q1 | 所有権をどこで確かめるか（A-2） | Spotify for Creators に倣い、**取り込みの流れの中で**確かめる（§8）。MVP は開発者 1 人なので確認しない |
| Q2 | 別の番組を取り込もうとしたとき（B-1） | 一般公開版は**番組を追加**する（Spotify for Creators と同じく、複数の番組を切り替えて管理する）。差し替えはしない。MVP は番組が 1 つなので、別の番組は取り込ませない（`import_other_show`） |
| Q3 | 追加済みの番組をもう一度取り込もうとしたとき（C-5） | 「この番組は追加済みです」で**取り込みの流れは終わる**。最新の情報を入れるのは、取り込みとは別の「読み込み直す」操作 |
| Q4 | 誤って取り込んだときに戻せるか（A-1） | **認証できた番組しか取り込めない**ので、誤って取り込むこと自体を防ぐ。戻す仕組みは作らない |

読み込み直しでの上書き（C-5 の後半）は **Issue #139** に切り出した。決まっているのは次の 3 点（ユーザー判断 2026-09-26）:
PodsNow だけの設定（概要欄・トークテーマのひな形、構成、既定のシーズン）は触らない／配信済みの回は配信先の値が正で上書きしてよい／未配信の回は RSS に無いので上書きされない。
番組そのものの情報は、録音だけの人と配信まで使う人で正本が違うので #139 で決める。

## 7. 実装の状況

| ケース | 状況 |
|---|---|
| B-1 / D-1 別の番組が混ざる | **対応済み【事実】。** `preview` が §5 の規則で `new` / `same` / `different` を判定し、`different` は `commit` が `import_other_show` で拒否する。画面は、追加済みなら「追加済みです」、別の番組ならエラーを出し、「取り込む」を出さない |
| Q3 追加済み | **対応済み【事実】。** 読み込み直しは `previewRefresh` という別の入口にした。前回の RSS が別の番組に変わっていたら拒否する |
| E-8 危険な URL | **対応済み【事実】。** RSS の解析時に http(s) 以外の URL を捨てる（`safeUrl`） |
| A-2 / Q1 / Q4 所有権の確認 | 一般公開版で、配信基盤（#107）と一緒に作る |
| 複数の番組（Q2） | 一般公開版。FR-SHOW-1（MVP は 1 Show 固定）を一般公開で変える |
| C-5 後半（読み込み直しでの上書き） | Issue #139 |
| E-1 / E-3 通信の異常・アートワークだけ失敗 | 対応済み【事実】（テストあり） |
| A-1 直近の回の表示、A-3 locked の案内、A-4 / F-2 他人の情報、B-4 手元の回との話数の重なり、B-5 配信基盤へ移ったあとの旧 RSS | 未着手。A-3 / B-5 は #107 と一緒に |
| C-2 取り下げの表示、C-3 / C-6 guid の振り直しによる重複 | 未着手 |
| D-2 話数の打ち間違いの注意、D-3 シーズンごとの話数 | 未着手 |
| E-2 429 の出し分け、E-4 途中終了で残るアートワーク、E-5 取り込み中の戻る操作、E-6 文字コード、E-7 数千回の計測、E-9 回の概要の HTML、E-10 長さの上限 | 未着手（E-9 は回の一覧を作るまで表示箇所が無い） |
| F-1 検索語を Apple に送る旨の表示 | 未着手 |

## 8. 所有権の確認（Spotify for Creators を参考に）【提案】

### 8.1 Spotify for Creators の流れ【確認済み】

1. 「既存の番組を探す」→「ほかのサービス（Somewhere else）」を選び、RSS の URL を入れる。
2. Spotify が RSS の `<itunes:email>`（`itunes:owner` の中）の宛先へ、8 桁の確認コードをメールで送る。送り先は Spotify のアカウントのメールではなく、**RSS に載っているメール**。
3. コードを入力すると、その番組を自分のアカウントで管理できる。

- 出典: [Claiming your podcast on Spotify for Creators](https://support.spotify.com/us/creators/article/claiming-your-podcast-on-spotify-for-creators/)（環境の制限で本文は開けず、検索結果の要約で確認）、[RSS.com: Claim your submitted show](https://help.rss.com/en/support/solutions/articles/44002440660-how-do-i-claim-my-rss-com-feed-on-spotify-for-creators-)、[Buzzsprout: Claim Your Spotify Listing](https://www.buzzsprout.com/help/221-claim-your-spotify-listing)
- 迷惑メール対策で、**RSS にメールを載せない配信サービスが増えている**。確認のときだけ一時的に載せる機能がある（RSS.com は 12 時間、Podbean は 48 時間で自動的に消える）【確認済み】([RSS.com](https://help.rss.com/en/support/solutions/articles/44002405274-how-to-enable-the-email-address-in-the-rss-feed)、[Podbean](https://blog.podbean.com/podbean-removes-emails-from-podcasters-rss-feeds-to-protect-podcaster-privacy-and-reduce-spam/)、[Transistor](https://transistor.fm/changelog/hide-email/))
- メールの代わりに、RSS に確認用の文字列を置く方式もある（`<podcast:txt purpose="verify">`）【確認済み】([podcast:txt](https://podcasting2.org/docs/podcast-namespace/tags/txt))

### 8.2 PodsNow にそのまま持ち込めない点【事実】

**確認コードは、アプリの外（サーバー）で作って照合しないと意味がない。**
アプリの中でコードを作ると、アプリ自身がコードを知っているので、改造すれば確認を飛ばせる。
メールの送信にも送信用の秘密情報が要り、アプリには埋め込めない（#107 §17 と同じ理由）。
したがって、確認は配信基盤（#107、Cloudflare Workers）の機能になる。

### 8.3 段階【事実】ユーザー判断（2026-09-26）

| 段階 | 取り込み | 確認 | 配信 |
|---|---|---|---|
| MVP（開発者 1 人。配信基盤なし） | できる（確認しない）。番組は 1 つ | しない | できない（配信基盤が無い） |
| 一般公開（配信基盤あり） | **確認できた番組だけ**取り込める（プレビュー → 確認 → 取り込む）。番組は複数持てる | メールのコード、または RSS の確認用文字列 | 確認済みの番組を配信できる |

### 8.4 確認の流れ（配信基盤ができたあと）

```
プレビュー
  └─ 「この番組の所有者か確認します」
       ├─ メールで確認（RSS にメールがあるとき）
       │    宛先は伏せて見せる（ta***@gmail.com）→ コードを送る → 8 桁を入力 → 確認済み
       ├─ RSS に文字列を置いて確認（メールが無い・届かないとき）
       │    PodsNow が文字列を発行 → 配信サービスの設定で <podcast:txt purpose="verify"> に入れてもらう
       │    → 「確認する」で配信基盤が RSS を取得して照合 → 確認済み
       └─（確認できなければ取り込まない。§9 の「録音だけ」の人は「新しく始める」で手で入力できる）
```

### 8.5 確認の準正常系・異常系

| ID | 状況 | 提案 |
|---|---|---|
| V-1 | RSS にメールが無い（配信サービスが隠している） | 「配信サービスの設定で、メールを一時的に RSS に載せてください」と案内し、主な配信サービスの手順へのリンクを出す。RSS の確認用文字列へ切り替えられるようにする |
| V-2 | RSS のメールが古い・届かない・迷惑メールに入る | 「迷惑メールフォルダを確認」を出す。再送は間隔を空ける（60 秒）【仮説】。届かなければ V-1 と同じく文字列方式へ |
| V-3 | RSS のメールが配信サービスの共通アドレス（本人に届かない） | 同上。文字列方式を勧める |
| V-4 | コードの入力間違い | 5 回で打ち切り、コードを無効にする【仮説】。やり直しは新しいコードで |
| V-5 | コードの期限切れ | 期限は 15 分【仮説】。期限切れなら再送 |
| V-6 | **他人の番組でコード送信を繰り返し、所有者にメールを送りつける**（嫌がらせ） | 番組ごと・端末ごとに送信回数を制限する（例: 1 番組 1 日 5 通）【仮説】。メール本文に「心当たりが無ければ無視してください。番組は変更されません」と書く |
| V-7 | 確認のあとで RSS のメールが変わった | 確認済みは取り消さない。配信基盤への移転時（#107）に改めて確認する |
| V-8 | 共同で運営している番組（複数人が確認したい） | MVP は 1 人。一般公開のときに、確認済みの人が招待する形を考える（#107 FR-PUB-8） |
| V-9 | 確認済みの番組とは別の番組を追加する（Q2） | 確認は番組ごと。追加する番組ごとに確認する |
| V-10 | 確認用文字列を RSS に入れたが、配信サービスのキャッシュで反映が遅い | 「反映まで数分かかることがあります」と出し、何度でも「確認する」を押せるようにする |
| V-11 | `podcast:locked` が yes | 確認とは別に、配信基盤へ移るときに locked を外す手順を案内する（A-3） |

### 8.6 データ【提案】

- 端末: `shows.ownership_verified_at` と、確認に使った方式。確認の結果は配信基盤が正本で、端末はその写し。取り込んだ番組は必ず確認済み（Q4）。手で作った番組は空
- 配信基盤: 確認コード（ハッシュで保存）・期限・試行回数・送信回数。メールアドレスそのものは保存しない（RSS から都度読む）。

## 9. 録音だけの人と、配信まで使う人【事実】

ユーザー指摘（2026-09-26）: PodsNow を使う人には、**録音だけしたい人**と、**配信まで PodsNow で行いたい人**がいる。

| | 録音だけ | 配信まで |
|---|---|---|
| 目的 | 録って・編集して・書き出す。配信は Spotify for Creators などの他社で行う（Distribution Pack でコピペ） | 録って・PodsNow の配信基盤から公開する（#107） |
| アカウント | **不要**（今の Local-first のまま） | 必要（配信基盤の番組と結ぶため） |
| 通信 | 無し（取り込み・読み込み直しのときだけ） | 公開・配信状況の取得のとき |
| 番組の作り方 | 手で入力（新しく始める）、または取り込み | 取り込み、または新しく作る（配信基盤が RSS を発行） |
| 取り込み | 確認できた番組だけ（Q4） | 同じ |

### 9.1 考え方

1. **録音だけの人に、アカウントや配信の言葉を見せない。** 配信は「書き出して共有」の横に置く選択肢の 1 つにし、選んだ人だけがアカウントを作る。PRODUCT.md の原則 2（Local-first）と 6（独自の言葉を作らない）を守る。
2. **確認とアカウントを分ける。** 所有権の確認（メールのコード）はサーバーで行うが、アカウントは作らない。確認済みという結果だけを端末に返す。こうすれば、録音だけの人も「アカウント無しで、自分の番組を取り込める」。
3. **途中で「配信まで」に移れる。** 録音だけで使っていた番組を、あとからアカウントに結んで配信基盤へ移せる。番組とエピソードの ID（UUID）は変えずに結ぶだけにし、データを作り直さない。取り込み時の確認が済んでいれば、改めてメールを確かめ直すかは #107 で決める。
4. **番組の数はどちらも複数持てる**（一般公開版。Q2）。録音だけの人が複数の番組を録ることもあるため。

### 9.2 決めたこと【事実】ユーザー判断（2026-09-26）

| # | 論点 | 決定 |
|---|---|---|
| Q5 | 録音だけの人の取り込みにも、確認（＝サーバーとの通信）を求めるか | **求める。ただしアカウントは作らせない**（9.1 の 2）。確認の結果だけを端末に返す |
| Q6 | どこで「配信まで使う」を選ばせるか | **書き出しタブの「共有」の横に「PodsNow から配信する」を置く。**押した人にだけアカウント作成と番組の確認を案内する。初回起動時に利用者の型を選ばせることはしない |

いずれも一般公開版（配信基盤あり）の仕様。MVP（開発者 1 人）では確認もアカウントも作らない。
