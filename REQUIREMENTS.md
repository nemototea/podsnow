# REQUIREMENTS.md — PodsNow 要件定義

> 凡例: **【事実】** 対話で決定した仕様 / **【確認済み】** 公式ドキュメント等で確認済み（出典付き） / **【仮説】** 未検証・要スパイク
>
> 優先度: **MUST**（MVP 必須） / **SHOULD**（MVP に入れたいが削れる） / **LATER**（MVP 後）

## 1. 用語

| 用語 | 意味 |
|---|---|
| Show | 番組。素材・既定構成・テンプレート・メタデータ既定値を持つ。MVP は 1 つ固定 |
| Episode | 1 回の配信 = 1 プロジェクト |
| Take | 録音ボタンを押してから停止するまでの 1 回の録音。1 つ以上の Segment（WAV ファイル）から成る。**内部名。UI では「録音」** |
| Segment | 連続して書き込まれた 1 つの WAV ファイル。割り込み等で分かれる |
| 声トラック | Take を切り貼りして並べた 1 本の連続した列（Voice Timeline）。**内部名。UI では「並び順」** |
| 素材（Asset） | Show に登録された Opening / Ending / ジングル / SE / BGM |
| オーバーレイ（Overlay Clip） | 声トラックの上に重ねた素材の配置。**内部名。UI では「素材」** |
| トークテーマ | エピソードで話す項目の列。各項目は見出しと、任意の本文（＝台本）を持つ。収録タブで閲覧・編集する（§2.2.1） |
| 台本 | トークテーマの項目に書いた本文。書かなければ見出しだけの項目になる。別のモードではない |
| チャプター | 録音中にトークテーマの項目へ進んだ位置。ユーザーが打つのではなく、項目を送ると記録される |
| 録音中の出来事 | 割り込み・機器の変更など、アプリが自動で記録する位置（`recording_events`） |
| Distribution Pack | 書き出し完了後の共有画面。**内部名。UI では「共有」** |

**用語の原則（NFR-9）**: UI に出す言葉は一般語だけにする。上の表で「内部名」と書いたものは `src/i18n/` に入れない。
サービス固有の言い回しはユーザーの認知負荷になり、理解のハードルを上げる。詳細と対応表は `docs/ux-restructure.md` §2 原則 1。

## 2. 機能要件

### 2.1 Show / Episode 管理

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-SHOW-1 | Show は 1 つ固定で自動作成される。UI に Show 切替は出さない | MUST | 【事実】MVP。**一般公開版では複数の番組を持ち、切り替えて管理する**（Spotify for Creators と同じ。ユーザー判断 2026-09-26、`docs/podcast-import-cases.md` Q2） |
| FR-SHOW-2 | データモデルは複数 Show を持てる構造にする（全エピソードに show_id） | MUST | 【事実】 |
| FR-SHOW-3 | Show は番組名・概要・著者・Cover Art・既定メタデータ・概要欄ベーステンプレート・既定構成（Opening/Ending/BGM の自動配置）を持つ | MUST | 【事実】 |
| FR-SHOW-3a | Show は Podcast RSS の番組（channel）の情報を持つ: Web サイト・言語・カテゴリー（主と副、複数）・explicit・番組の形式（episodic / serial）・著作権表記・所有者の名前とメール・完結済み・RSS の URL・`podcast:guid`・支援リンク。外部サービスの番組 ID は PodsNow の ID と分けて持つ（DATA_MODEL.md §4.1） | MUST | 【事実】ユーザー判断（2026-09-25）。取り込み（FR-SHOW-6〜）と将来の RSS 生成の両方の受け皿。値の範囲は【確認済み】PSP-1（https://github.com/Podcast-Standards-Project/PSP-1-Podcast-RSS-Specification） |
| FR-SHOW-4 | Show に**トークテーマのひな形**（見出しの列）を 1 つ登録でき、新規エピソードへ自動で入る。入った後はそのエピソードのデータなので、自由に書き換え・削除できる | MUST | 【事実】毎回同じ並びで話す番組の定型作業を消す。`docs/ux-restructure.md` §4.4 |
| FR-SHOW-5 | Home 上部の番組カードを、番組設定への唯一の入口にする。番組設定は「番組 → 素材 → 新しいエピソードの構成 → トークテーマのひな形 → 概要欄テンプレート」の順にし、素材には見出しと登録件数つきのカードを置く。素材の登録・試聴・並べ替えは子画面にまとめ、既定構成の素材選択からも対象用途を選んだ状態で進める | MUST | 【事実】Issue #130。アプリ全体の設定から番組への重複した入口を外し、番組の文脈に置く |
| FR-SHOW-6 | 番組をまだ設定していない Home は仮の番組名を主見出しにせず、「配信中の番組を取り込む」と「新しく始める」を選べるセットアップカードを出す。エピソードを先に作ってもカードは残し、取り込み済みまたは「新しく始める」を選んだときに番組カードへ切り替える。画面をふさがず、録音開始までの手数は増やさない（FR-EP-2）。取り込みは番組設定からいつでも開ける（取り込み後は「番組の情報を読み込み直す」） | MUST | 【事実】Issue #101 / #130。`src/app/import.tsx` |
| FR-SHOW-7 | 番組名で検索して、配信中の番組を選べる。検索は Apple の公開検索（iTunes Search API。API キー不要）をアプリから直接呼び、主に RSS の URL（`feedUrl`）を見つけるために使う | MUST | 【事実】Issue #101 §5。検索元は差し替えられる形にする（Podcast Index 等） |
| FR-SHOW-8 | 検索で見つからないときは、RSS の URL を直接入力して取り込める。検索経由と同じ取り込み処理を通る | MUST | 【事実】Issue #101 §11 |
| FR-SHOW-9 | 取り込む前に内容（アートワーク・番組名・概要・著者）を確認でき、確定するまで DB に書かない。同じ項目は RSS を優先し、RSS に無い項目だけ検索結果で補う。アートワークはダウンロードして `cover_path` に保存する（オフラインで表示し、書き出しに埋め込むため） | MUST | 【事実】Issue #101 §4 / §8 |
| FR-SHOW-10 | RSS の配信済みの回（item）を `feed_episodes` に取り込む。音声はダウンロードしない。再取り込みは `guid` で突き合わせて上書きし、フィードから消えた回は消さない | MUST | 【事実】ユーザー判断（2026-09-25）。DATA_MODEL.md §4.17 |
| FR-SHOW-11 | 取り込めたことを番組の所有者である証明とはみなさない。取り込んだ番組を PodsNow の配信基盤から配信する前に、所有権を確認する（FR-PUB-6） | MUST | 【事実】Issue #101 §14。配信（§2.13）で必須になった |
| FR-SHOW-12 | 取り込みの準正常系・異常系（別の番組の取り込み、読み込み直しでの上書き、所有権、通信・入力の異常）は `docs/podcast-import-cases.md` のケースと判定規則に従う | MUST | 【事実】ユーザー指摘（2026-09-26）。規則の細部は同文書 §6 で未決 |
| FR-EP-1 | Home は上部に現在の番組を簡潔に示すカード、その下に全エピソードを同じ一覧として表示する。「続きのエピソード」だけを別カードで重複表示しない | MUST | 【事実】Issue #130。各回は一覧から選んで編集できる |
| FR-EP-2 | 新規エピソード作成時、Show の既定構成を自動適用し Editor へ遷移。Home から録音開始まで 3 タップ以内 | MUST | 【事実】 |
| FR-EP-3 | エピソードの状態（Draft / Ready / Exported）はアプリが自動判定し、Home の一覧行ではアイコンと読み上げで示す | SHOULD | 【事実】Issue #130 |
| FR-EP-4 | エピソードの複製と、2 種類の削除を持つ。**音声を削除**（録音だけ消し、行・話数・メタデータ・書き出し履歴は残す）と**エピソードを削除**（行ごと。Undo 可能、元の録音ファイルは保持期間内は残す） | SHOULD | 【事実】容量目的の削除と、試用の後片付けは意図が違う。§2.1.1 |
| FR-EP-5 | **廃止。** エピソードのトップ画面（工程 5 行）は持たない。1 エピソード = 1 画面で、上部のタブ **録音 / 編集 / 書き出し** を切り替える。録音の一覧は「録音」タブの下部に置く | MUST | 【事実】同 §3。到達経路の重複（工程 5 画面に対して 24 本）を断つ |
| FR-EP-6 | 話数は作成時に「そのエピソードより後の番号が存在しない位置」へ自動採番し、以後ユーザーが自由に上書きできる。採番はカウンターを持たず既存行から導出する | MUST | 【事実】§2.1.1 |

#### 2.1.1 話数の採番【事実】

新規作成時の話数は、次の式で求める。**例外を持たない**（`status` も削除済み行も条件に入れない）。

```sql
SELECT MAX(
  COALESCE((SELECT MAX(episode_number) FROM episodes      WHERE show_id = ? AND deleted_at IS NULL), 0),
  COALESCE((SELECT MAX(episode_number) FROM feed_episodes WHERE show_id = ?), 0)
) + 1
```

- 「一覧に見えている最大の話数の次」なので、ユーザーが画面を見て結果を予測できる。
- 採番用のカウンター列は持たない。台帳は `episodes` の行と、配信済みの回 `feed_episodes`（取り込んだ回と、PodsNow の配信基盤から配信した回。DATA_MODEL.md §4.17）。
- **配信済みの番号は二度と使わない**。取り込んだ番組は続きの番号から始まる（第 120 回まで配信済みなら次は第 121 回）。ユーザー判断（2026-09-25）。
- 配信済みの回に話数（`itunes:episode`）が無い番組では、この式は取り込みの影響を受けない。取り込み画面では「RSS に話数がありません。新しいエピソードの話数は、作ったあとで直せます」と伝える（最初の 1 回を手で直せば、以後はその行から導出される）。取り込み時に「次は第何回から」を聞く案は、入力の置き場所がカウンターの復活になるので採らなかった【事実】。
- 採番はあくまで初期値の提案であり、最終的な権限はユーザーの手入力にある（FR-META-1）。重複は警告するが禁止しない（再収録・シーズン運用があるため）。

**`status = 'exported'` を採番の条件に入れない理由**: 書き出しはファイル出力であって公開ではなく、書き出しだけでは「世に出たか」を知り得ない（世に出たことは `feed_episodes` が表す）。そこへ規則を乗せると、試し書き出しを捨てたときに永久欠番が出るか、公開後にローカルを整理したときに番号が再利用されるかの、どちらかが必ず壊れる。アプリが意図を推測するのをやめ、FR-EP-4 の 2 種類の削除としてユーザーに表明してもらう。

**受け入れ基準**

| シナリオ | 期待 |
|---|---|
| 試しに作って消す | #1 → 削除 → 次の新規も #1 |
| 書き出し済みを「音声を削除」 | 容量が空き、#5 は一覧に残る |
| 書き出し済みを「エピソードを削除」 | #5 が返る。削除前に「番号は再利用される」と伝える（禁止はしない） |
| 削除 → 新規作成 → Undo で復元 | 元番号のまま戻す。衝突する場合のみ MAX+1 に振り直して通知 |
| 配信中の番組を取り込む（第 120 回まで配信済み） | 次の新規は #121 |
| 配信済みの #121 に対応する手元の回を削除 | #121 は返らない（次も #122） |
| ストレージクリア後に作成 | #1 から。ユーザーが 1 回手で直せば以降は追従する |

設定画面に「次の話数」の欄は置かない。カウンターを別の形で復活させることになり、ストレージクリアで復元不能になる問題が戻るため。

### 2.2 収録

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-REC-1 | 録音ボタンは収録タブの下部に常設。押すと再生位置から新しい Take を録る。再生位置が途中なら**差し込む**（後ろの声はずれ、何も消えない）。末尾なら足す。止めたら、今録った部分の直後に再生位置を置く（続けて押せば続きから録れる）。録音中・一時停止中は位置を動かせない | MUST | 【事実】一般的な収録アプリの挙動に寄せる。Issue #122 のユーザー判断（2026-09-25） |
| FR-REC-2 | 一時停止 / 再開。同一 Take として続きを録る | MUST | 【事実】 |
| FR-REC-3 | 画面ロック中・他アプリ表示中も録音を継続する | MUST | 【事実】 |
| FR-REC-4 | **廃止（マーカー、言い直す）。** 言い間違えたら、止める → 波形で塊を選んで削除 → 空いた位置（再生位置が置かれる）から録る（FR-REC-1）。録音中に直近を捨てる独自の操作は持たない | — | 【事実】マーカーは押しても何も解決しなかった（`docs/ux-restructure.md` §1.3）。代わりの「言い直す」も分かりにくかった。Issue #122 のユーザー判断（2026-09-25） |
| FR-REC-5 | 収録中にジングル / SE をワンタップで挿入イベントとして記録し、手元でモニター再生する。マイクに回り込ませない（書き出し時にミックス） | MUST | 【事実】 |
| FR-REC-6 | 収録タブでトークテーマと台本を**いつでも**閲覧・編集できる（録音前・録音中・録音後）。録音中は現在の項目を大きく表示し、次の項目へ送れる。**収録スタイルのような設定項目は持たない**（§2.2.1） | MUST | 【事実】同 §4 |
| FR-REC-7 | トークテーマは OS のキーボード音声入力で入力できる（アプリ固有実装なし） | SHOULD | 【事実】OS 標準で済むものは利用 |
| FR-REC-8 | 録音レベルメーターを表示。クリップ（過大入力）を警告 | MUST | 対談・外部マイク対応のため |
| FR-REC-9 | 入力ソース（内蔵 / 有線 / Bluetooth / オーディオインターフェース）を表示し、選択できる | MUST | 【事実】 |
| FR-REC-10 | Bluetooth マイク選択時は音質低下（通話プロファイル）を警告する | SHOULD | 【仮説】HFP の帯域制限。実機で確認 |
| FR-REC-11 | 録音経過時間、残りストレージ目安を表示 | MUST | データ損失防止 |
| FR-REC-12 | 録音品質: 48 kHz / 16 bit を既定。ステレオ既定、モノラルはオプション（容量を約半分にする。一人語り専用とは限らないため） | MUST | 【事実】非圧縮保存 |
| FR-REC-13 | スピーカー出力中にジングルをモニター再生すると回り込む旨を警告し、「イヤホン接続時のみ再生 / ミュートで挿入だけ記録」を選べる | SHOULD | 【仮説】 |

#### 2.2.1 トークテーマと台本【事実】

**「収録スタイル」という設定項目は作らない。** 選ばせると、選ぶ前に選択肢の意味を理解させることになる。実際には選ぶ必要がない。

- トークテーマだけ書く → 見出しの列ができる
- 台本も書く → その項目に本文が付く
- 何も書かない → フリートーク。**「台本なし」を状態として持たない。入力しなければそれがフリートーク**

つまり **1 つのリストがあるだけ**で、項目ごとに本文があったりなかったりしてよい。

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-OUT-1 | エピソードは「話す項目」の列を持つ。各項目は**見出し（必須）**と**本文（任意 = 台本）**からなる。スタイルやモードを表す列は持たない | MUST | 【事実】 |
| FR-OUT-2 | 項目は収録タブで、録音前・録音中・録音後のいつでも追加・編集・並べ替え・削除できる。準備のための専用画面は作らない | MUST | 【事実】現在はエディタのシートでしか書けない |
| FR-OUT-3 | **複数行をまとめて貼り付けると、改行で項目に分割される。** 並べ替えはドラッグ（FR-UI-1）。入力は OS のキーボード音声入力が使える（FR-REC-7） | MUST | 【事実】1 行ずつの送信は 10 項目で 10 回の操作になる |
| FR-OUT-4 | 録音中に次の項目へ送ると、その位置が**チャプター**として記録される（ユーザーは何も打たない）。チャプターは収録タブの波形で位置の指定に使う | MUST | 【事実】マーカー（FR-REC-4）の置き換え |
| FR-OUT-5 | 項目の見出しは概要欄テンプレートの `{{topics}}` に箇条書きとして差し込める | MUST | 【事実】現行 FR-META-3 の入力元 |

### 2.3 データ保全（収録中の異常系）

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-SAFE-1 | 録音は非圧縮 PCM（WAV）で保存する | MUST | 【事実】 |
| FR-SAFE-2 | WAV ヘッダ（データ長）を一定間隔（目安 1 秒）で更新し、fsync する。プロセスが突然終了しても直前までのデータが再生可能な状態を保つ | MUST | データ損失防止 |
| FR-SAFE-3 | 録音開始時に「録音中」を示す復旧ジャーナルを DB に書く。次回起動時に未確定の Take を検出し、ファイル実長からヘッダを修復して Take を復元する | MUST | データ損失防止 |
| FR-SAFE-4 | 電話着信・他アプリの Audio Session 割り込みで録音が止まった場合、現在の Segment を確定し、割り込み終了後に自動または手動で新しい Segment として再開できる。ユーザーに割り込みが起きた位置を示す | MUST | 【事実】 |
| FR-SAFE-5 | 録音開始前に必要ストレージ（想定 60 分分 + 余裕）を確認し、不足なら警告。録音中も監視し、しきい値を下回ったら安全に停止して通知する | MUST | 【事実】 |
| FR-SAFE-6 | 入力デバイスが抜かれた（Route change）場合も録音を継続し、その位置に自動マーカーを打つ | SHOULD | 【仮説】 |
| FR-SAFE-7 | すべての編集は非破壊。元の Segment ファイルは編集で変更・削除されない | MUST | 【事実】 |
| FR-SAFE-8 | 編集操作は即時に永続化する（自動保存）。画面を離れるときに保存の確認は出さない。取り消しの履歴は画面を開いている間だけ持つ（FR-EDIT-7） | MUST | 【事実】履歴を再起動後まで持つ要件は Issue #122 のユーザー判断（2026-09-25） で削った |
| FR-SAFE-9 | Android では録音をフォアグラウンドサービス（通知付き）として実行する | MUST | 【確認済み】Android 14+ の `microphone` FGS 要件 (AUDIO_DESIGN.md 参照) |

### 2.4 編集

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-EDIT-1 | タイムラインは「声トラック 1 本 + 素材レイヤー」。声は Take の切り貼りの列であり、ドラッグで自由配置はしない | MUST | 【事実】 |
| FR-EDIT-2 | 範囲を選んで削除する。**選択は「無音で区切られた声の塊」をタップして作り、左右のハンドルをドラッグして広げる**（隣の塊の境界にスナップ、長押しで 0.1 秒刻み）。再生位置を動かして「ここから」「ここまで」を押す方式は廃止する。削除は確認のうえ実行し（FR-UI-2）、直後に「取り消す」を表示 | MUST | 【事実】同 §1.4 / §6.2。確認を挟むのは Issue #121 のユーザー判断 |
| FR-EDIT-3 | 無音の自動検出と一括カット。しきい値（長さ・音量）は設定で変更可。実行前にプレビュー件数を表示 | MUST | 【事実】 |
| FR-EDIT-4 | **いったん廃止（Take の並び替え）。** 並べ替えの一覧（「この回の録音」）は、1 行が Take ではなく声の並びの 1 区間で、範囲削除で割れて増え、何の一覧か分からなかった。位置を選んで録る（FR-REC-1）で代わりが効く | — | 【事実】Issue #122 のユーザー判断（2026-09-25）。必要になれば波形上の操作として考え直す |
| FR-EDIT-5 | **廃止（部分録り直し「録り直す」）。** 塊を選んで削除すると、その位置に再生位置が置かれる。そのまま録音を押せば差し込みで埋め直せる（FR-REC-1） | — | 【事実】Issue #122 のユーザー判断（2026-09-25） |
| FR-EDIT-6 | 素材の後挿入、位置調整、音量調整、フェードイン/アウト | MUST | 【事実】 |
| FR-EDIT-7 | Undo / Redo。連続操作（スライダー等）は 1 回にまとめる。**エピソード画面の上部に常に置き、どのタブからも使える。** 録音中（準備・一時停止・割り込み・保存中を含む）は押せない（トーストの「取り消す」も効かない）。対象は声の並びと重ねた素材で、**録音の追加も 1 つの操作**（録音中に重ねた素材を含む）。取り消すとテイクが声の並びから外れ、録音ファイルは残る。取り消したあとに新しく操作すると、やり直し側は捨てる。履歴はエピソード画面を開いたときと抜けたときに空にする。トークテーマは対象外（削除の確認で守る、FR-UI-2） | MUST | 【事実】Issue #122 のユーザー判断（2026-09-25）。録音の追加を履歴の外で書いていたため、録音後に取り消すと録ったテイクが外れた |
| FR-EDIT-8 | 波形表示（声 / 素材）。60 分の素材でもスクロール・ズームが滑らか | MUST | 長尺対応 |
| FR-EDIT-9 | 再生（タイムライン全体をリアルタイムでミックス再生）| MUST | 編集確認のため。【仮説】実装方式は AUDIO_DESIGN.md |
| FR-EDIT-10 | チャプター（トークテーマ由来）を波形の上に並べ、タップでジャンプ、長押しでまるごと選択できる。マーカーの一覧と「次へ」は廃止 | SHOULD | 【事実】同 §6.3 |
| FR-EDIT-11 | 編集履歴の一覧画面 | LATER | 【事実】MVP 外 |

### 2.5 音の仕上げ

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-SND-1 | 書き出し時にラウドネス正規化（目標 -16 LUFS、トゥルーピーク -1 dBTP 目安）を適用。ON/OFF 可 | MUST | 【事実】 |
| FR-SND-2 | BGM ダッキング（声がある区間で BGM の音量を下げる）。下げ幅・アタック/リリースは既定値、Advanced で調整 | MUST | 【事実】 |
| FR-SND-3 | ノイズ除去、サウンドスタイル、コンプレッサー、EQ | LATER | 【事実】 |

### 2.6 詳細（メタデータ）

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-META-1 | タイトル、概要、話数、季、収録日、公開予定日（任意） | MUST | 【事実】 |
| FR-META-2 | 概要欄ベーステンプレートを新規エピソードに自動適用し、都度編集できる。テンプレートは Show 設定で編集 | MUST | 【事実】 |
| FR-META-3 | 概要の一部は「トークテーマ」から箇条書きを差し込める（ボタン操作） | SHOULD | 【事実】将来 AI が下書きを差し込む場所と同じ UI |
| FR-META-4 | 将来、AI が生成した「下書き候補」を同じ欄に提案できる構造（提案 → 採用/破棄）| LATER | 【事実】 |
| FR-META-5 | エピソードは Podcast RSS の回（item）の情報を持つ: `guid`（作成時に決めて変えない）・種類（本編 / 予告 / おまけ = full / trailer / bonus）・explicit（未指定なら番組に従う）・Web ページ・実際の配信日時（公開予定日とは別） | MUST | 【事実】ユーザー判断（2026-09-25）。DATA_MODEL.md §4.5。画面での編集は SHOULD |
| FR-META-6 | 配信済みかどうかは `published_at`（配信済みの回と結びついたか）で判定し、一覧とホームのカードに出す。`status`（Draft / Ready / Exported）は変えない | SHOULD | 【事実】配信（§2.13）で「世に出たか」を知れるようになった |

### 2.7 書き出し・共有

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-EXP-1 | 書き出し形式: M4A (AAC) と WAV | MUST | 【事実】 |
| FR-EXP-2 | MP3、FLAC | LATER | 【事実】【確認済み】Android に MP3 エンコーダなし |
| FR-EXP-3 | 品質プリセット（Podcast 128 kbps / High 256 kbps / カスタム）と推定ファイルサイズ表示。**選んだプリセットはエピソードごとに覚え**、タブの切り替え・画面を閉じる・再起動の後も同じ選択で開く。選んだことのない回は設定の既定（FR-SET-5）で開く。複製は選択を引き継ぐ。カスタムの中身は回ごとには持たず設定に 1 つ | MUST | 参考: バンドル。【事実】Issue #136。DATA_MODEL.md §4.5.1 |
| FR-EXP-4 | 書き出しはバックグラウンドで進行し、進捗を表示。キャンセル可 | MUST | 長尺対応 |
| FR-EXP-5 | 書き出し履歴（日時・形式・サイズ・ファイル参照） | SHOULD | 参考: バンドル |
| FR-EXP-6 | OS 共有シートで送る（AirDrop、他アプリ）| MUST | 【事実】 |
| FR-EXP-7 | ファイルアプリ（iOS Files / Android Downloads 等）へ保存 | MUST | 【事実】 |
| FR-EXP-8 | Distribution Pack: タイトル / 概要 / メタデータ一式を個別コピー | MUST | 【事実】 |
| FR-EXP-9 | **廃止。** エピソード単位の `.podsnow` バックアップと復元は、番組単位の端末移行・同期や配信基盤の単位に合わず、十分な保全にならない。将来は #65 で番組単位の制作データ同期として設計する。録音中断からの自動復旧（FR-SAFE-3）は維持する | — | 【事実】Issue #130。ホスティング済みの完成音声からテイク・編集履歴・素材配置は復元できない |
| FR-EXP-10 | ID3 / MP4 メタデータ（タイトル、アーティスト、アルバム、Cover Art）の埋め込み | SHOULD | 【仮説】 |

### 2.8 Show Assets

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-AST-1 | 用途別（Opening / Ending / ジングル / SE / BGM）に素材を登録。端末のファイルから取り込み | MUST | 【事実】 |
| FR-AST-2 | 試聴、お気に入り（Quick Insert の先頭に出る）、並び替え、削除 | MUST | 参考: バンドル |
| FR-AST-3 | 取り込み時に内部形式（WAV 48 kHz）へ変換しキャッシュする | SHOULD | 【仮説】ミックス処理の単純化のため |

### 2.9 設定

| ID | 要件 | 優先度 |
|---|---|---|
| FR-SET-1 | テーマ: Dark / Light / System | MUST |
| FR-SET-2 | 録音品質、モノラル/ステレオ、入力ソースの既定 | MUST |
| FR-SET-3 | 無音しきい値（長さ・音量）、無音を自動で詰めるか | MUST |
| FR-SET-4 | ハプティクス ON/OFF | SHOULD |
| FR-SET-5 | 既定の書き出しプリセット（書き出しタブでまだ選んでいない回に使う。番組ごとの既定は持たない。FR-EXP-3） | MUST |
| FR-SET-6 | ストレージ使用量の表示、書き出し済みエピソードの元データ整理 | MUST |
| FR-SET-7 | アクセシビリティ（文字サイズ追従、読み上げラベル）| SHOULD |
| FR-SET-8 | 表示言語: システムに合わせる / 日本語 / English | MUST |

### 2.10 ローカライゼーション（Issue #80）

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-I18N-1 | 対応言語は日本語と英語。フォールバックは英語 | MUST | 【事実】 |
| FR-I18N-2 | 既定では端末の言語設定に従う。OS の優先順で最初に対応している言語を使い、どれも対応外なら英語 | MUST | 【確認済み】`expo-localization` の `getLocales()` は OS の優先順で返す（node_modules の型定義で確認。docs.expo.dev は本作業環境からは参照不可） |
| FR-I18N-3 | 設定画面から言語を手動で上書きできる。切り替えは再起動なしで即反映（テーマと同じ挙動） | MUST | 【事実】 |
| FR-I18N-4 | ユーザーに見える文言は `src/i18n/` に集約する。`domain` / `services` / `infra` は文言を持たず、エラーは `AppErrorCode` で返す。境界は ESLint で強制する | MUST | ARCHITECTURE.md §2 |
| FR-I18N-5 | 日本語と英語のキー集合が一致していることをテストで保証する | MUST | 【事実】翻訳漏れを実行時に出さない |
| FR-I18N-6 | DB に書き込む既定文言（Show 名・概要欄テンプレート・エピソードタイトル・Take 名・割り込みマーカー）は初回作成時の表示言語で作る。作成済みの行はユーザーのデータなので、言語を切り替えても書き換えない | MUST | 【事実】 |
| FR-I18N-7 | OS の権限ダイアログ（マイク）を端末の言語で出す | SHOULD | 【仮説】`app.json` の `expo.locales` で iOS の `InfoPlist.strings` を生成する。実機で未検証 |
| FR-I18N-8 | Android 13+ の「アプリごとの言語」設定に対応する（`locales_config.xml`） | SHOULD | 【仮説】expo-localization の config plugin が生成する。実機で未検証 |

### 2.11 AI 補助（接続点のみ）

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-AI-1 | 文字起こし結果を Take に紐づけて保存できるテーブルを用意（MVP では未使用）| MUST（スキーマのみ） | 【事実】 |
| FR-AI-2 | 「要約 / 概要欄下書き」の提供元をインターフェースで抽象化（OS 標準 / ローカル LLM / なし）| MUST（設計のみ） | 【事実】無料・ローカル運用が前提 |
| FR-AI-3 | OS 標準の音声認識（iOS Speech / Android SpeechRecognizer）による文字起こし | LATER | 【仮説】オフライン可否・日本語精度は端末依存。要検証 |

### 2.12 共通の操作（Issue #119 / #121）

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-UI-1 | **並べ替えはアプリ全体で、行の右端のつまみのドラッグに揃える**（トークテーマ・この回の録音・素材）。上下ボタンやメニューの「上へ移動」は置かない。読み上げ中は行の操作「上へ移動」「下へ移動」で代替する（FR-SET-7）。録音中はこの回の録音を並べ替えられない | MUST | 【事実】ユーザー判断（2026-09-25）。操作を 1 つに揃える |
| FR-UI-2 | **削除の前に確認する**。対象はトークテーマ・素材・エピソード（書き出し済みかどうかに関係なく）・収録タブの範囲削除・重ねた素材を外す。確認のあとのトーストの「取り消す」は残す | MUST | 【事実】ユーザー判断（2026-09-25）。取り消しの効かないトークテーマも、取り消せる操作も、同じ作法にそろえる |

### 2.13 配信（PodsNow の配信基盤）【事実】ユーザー判断（2026-09-25）

PodsNow 自身が RSS と音声をホスティングし、「録る → 公開する」をアプリで完結させる。設計は **Issue #107**（Cloudflare Workers + R2 + CDN、`feeds.podsnow.app` / `media.podsnow.app`、署名付きアップロード URL）。
MVP は**開発者自身の番組 1 つ**で配信する。うまく配信できると分かったら、一般向けの公開を本格的に進める。
一般公開はアカウント・課金・保存容量・権利侵害の対応を伴う（#107 §18 / §21）ので MVP には入れない。ただし URL・ID・API は一般公開で作り直さずに済む形にしておく（#107 の設計原則）。

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-PUB-1 | アプリから配信基盤に認証して操作できる。MVP は開発者 1 人なので、配信基盤が発行したトークンを設定から登録する。一般公開ではアカウントに置き換える（FR-PUB-8） | MUST | 【事実】トークンは OS の安全な保管領域に置く（NFR-11） |
| FR-PUB-2 | 書き出した音声を署名付き URL でストレージへ直接アップロードし、メタデータを送って回を公開できる。公開後のメタデータ更新と、公開の取り下げもできる。ストレージの秘密鍵はアプリに持たない | MUST | 【事実】#107 §17。ARCHITECTURE.md §7.4 の `Publisher` |
| FR-PUB-3 | RSS は配信基盤が生成する（RSS 2.0 + iTunes + Podcast 名前空間。PSP-1）。番組ごとに固定の URL。回の `guid` は `episodes.guid`、番組の `podcast:guid` は `shows.podcast_guid`、`podcast:locked` は yes。RSS にストレージ事業者の URL を出さない | MUST | 【事実】#107 §5〜§7。`guid` を変えるとリスナーのアプリで回が重複する |
| FR-PUB-4 | 既存の配信サービスからの乗り換え: 取り込んだ過去の回（`feed_episodes`）を、`guid`・`podcast:guid` を変えずに新しい RSS に載せる。旧 RSS から新 RSS への転送（HTTP 301、または旧 RSS への `itunes:new-feed-url`）の手順を案内する | MUST | 【事実】【仮説: 各サービスの転送手順は乗り換え時に確認】 |
| FR-PUB-5 | 配信状況（公開済みの回、配信日時、音声の URL）をサーバーから取得し、`feed_episodes` と `episodes.published_at` を更新する。ユーザーの操作で行う（NFR-2） | SHOULD | 【事実】書き出しと配信を区別できる（FR-META-6） |
| FR-PUB-6 | 取り込んだ番組を配信する前に所有権を確認する。MVP は自分の番組なので手順の確認で足りるが、一般公開の前に仕組みを作る（旧 RSS の `podcast:txt` に確認用の値を置く、`itunes:owner` のメールで確認する等） | MUST（一般公開前） | 【事実】FR-SHOW-11。他人の番組を乗っ取れてはいけない |
| FR-PUB-7 | 音声はアプリケーションサーバーを通さず CDN から配信し、Range / HEAD リクエストに応える | MUST | 【事実】#107 §15 と受け入れ基準 |
| FR-PUB-9 | 配信は書き出しタブの「共有」の横の「PodsNow から配信する」から始める。押した人にだけアカウント作成と番組の確認を案内し、録音だけの人にはアカウントや配信の言葉を見せない。所有権の確認（取り込み時）はアカウント無しで行い、確認の結果だけを端末に返す | MUST（一般公開版） | 【事実】ユーザー判断（2026-09-26）。`docs/podcast-import-cases.md` §9 Q5 / Q6 |
| FR-PUB-8 | アカウント・課金・複数ユーザー・権利侵害の対応・ダウンロード数の計測・他社への移行（301）・一括エクスポート | LATER | 一般公開の判断のあと。#107 §11〜§13 / §18〜§21 |

配信基盤そのもの（Workers / R2 / RSS 生成）は Issue #107 で実装する。アプリ側はこの節と ARCHITECTURE.md §7.4。

**順番【事実】ユーザー判断（2026-09-25）:** 取り込み（FR-SHOW-6〜10、Issue #101）を先に作る。既存の番組の過去の回と話数を持った状態で配信基盤へ移るため。#107 本文 §24 で Phase 2 だった RSS Import / 乗り換えは前倒しになる。

## 3. 非機能要件

| ID | 要件 | 根拠 |
|---|---|---|
| NFR-1 | **データ損失ゼロ**: クラッシュ、OS によるプロセス終了、割り込み、ストレージ不足のいずれでも、直前 1 秒以内までの録音が復元できる | 【事実】最優先 |
| NFR-2 | **番組の取り込み（FR-SHOW-6〜10）と配信（§2.13）を除き**、オフラインで全機能が動作する。通信はユーザーが操作したときだけ行い、バックグラウンドで勝手に通信しない。取り込んだ・配信した情報は端末に保存するので、その後もオフラインで使える | 【事実】Local-first。ユーザー判断（2026-09-25）で取り込みと配信だけ例外にした |
| NFR-3 | 60 分の素材（48 kHz / 16 bit モノラル ≈ 330 MB）を扱っても UI が固まらない。波形はダウンサンプリング済みキャッシュから描画 | 長尺対応 |
| NFR-4 | 書き出し（60 分、ラウドネス正規化込み）は実機で数分以内。バックグラウンドで継続 | 【仮説】性能目標は Phase 0 スパイクで測る |
| NFR-5 | 個人データは端末外に送らない。解析・クラッシュレポート送信は MVP で行わない。例外は次の 3 つで、いずれもユーザーが操作したときだけ: **検索語（番組名）を Apple に**、**RSS の URL への GET を配信元に**、**公開すると決めた回の音声とメタデータを PodsNow の配信基盤に**。録音の素材・編集データは送らない | プライバシー |
| NFR-10 | 外部から取る RSS は信用しない入力として扱う。HTTPS のみ、タイムアウトとサイズ上限を持ち、リダイレクト先も HTTPS に限る。XML は外部実体を展開しない。取り込んだ HTML（概要）は表示時にタグを除くか安全に描画する | 【事実】Issue #101 §13。上限値は【仮説】実装時に決める |
| NFR-11 | 配信基盤の認証情報（トークン）は SQLite に置かず、OS の安全な保管領域（iOS Keychain / Android Keystore）に置く。将来の制作データ同期にも含めない | 【事実】【仮説: `expo-secure-store` を使う。SDK 57 の API は実装時に確認】 |
| NFR-6 | iOS / Android で同じタイムラインから同じ書き出し結果（サンプル単位で一致が理想、±1 LSB 程度を許容）が得られる | 【仮説】DSP を共通コードにするか否かの判断材料 |
| NFR-7 | 対応 OS: Expo SDK 57 がサポートする範囲 | 【確認済み】SDK 57 = RN 0.86 (https://expo.dev/changelog/sdk-57)。最小 OS バージョンは【仮説】要確認 |
| NFR-8 | UI に表示言語がハードコードされていない。新しい文言を足すときは日英の両方が揃わないとビルドが通らない | 【事実】`Messages = typeof ja` を `en.ts` に課している |
| NFR-9 | **UI に独自の言葉を作らない。** 画面に出す言葉は一般語だけにする（録音 / 編集 / 書き出し / 共有 / 素材 / トークテーマ / 台本 / チャプター）。内部名（Take / 声トラック / オーバーレイ / Distribution Pack / Outline）は `src/i18n/` に入れない。ボタンは「動詞 + 目的語」の普通の形にする | 【事実】固有の言い回しは認知負荷になり、理解のハードルを上げる。対応表は `docs/ux-restructure.md` §2 原則 1 |

## 4. 主要ユーザーフロー 【事実】

1. **今日の回を録って出す**: ホーム → ＋ → 収録タブ（毎回の素材とトークテーマのひな形が入った状態）→ ● 録音 → 停止 → 同じ画面で編集 → 書き出しタブ（タイトルと概要 → 音 → 書き出す）→ 共有 → 配信アプリへ
2. **途中から再開する**: ホーム → カードのボタン 1 つ → 同じタブへ（自動保存した状態から。取り消しの履歴は持ち越さない）
3. **言い間違いを直す（収録中）**: ■ 止める → 波形でその塊をタップ → 削除 → ● 録音（空いた位置に差し込まれる）
4. **言い間違いを直す（収録後）**: 収録タブ → チャプターでその辺りへ → 塊をタップ → 削除 →（必要なら）そのまま ● 録音
5. **割り込みから復帰する**: 着信 → 録音が自動停止（Segment 確定）→ 通話終了 → 「録音を再開しますか？」→ 再開（新 Segment）
6. **クラッシュから復帰する**: 再起動 → 「未確定の録音が見つかりました」→ 復元 → Editor へ

## 5. MVP 判定表

| 機能 | MVP | 備考 |
|---|---|---|
| 1 Show 固定・複数対応モデル | ✅ | |
| 収録（BG 継続 / 一時停止 / 位置を選んで差し込み / ジングル挿入 / トークテーマと台本） | ✅ | |
| 入力ソース選択・レベルメーター | ✅ | |
| 非圧縮保存・復旧ジャーナル・割り込み対応 | ✅ | |
| 声 1 本 + 素材レイヤー編集、Undo/Redo | ✅ | |
| 3 タブ化（録音 / 編集 / 書き出し）と導線の統合 | ✅ | `docs/ux-restructure.md` §3 |
| 塊の選択とチャプター | ✅ | 同 §6 |
| UI 文言の一般語化（NFR-9） | ✅ | 同 §2 原則 1 |
| 無音自動検出 | ✅ | |
| ラウドネス正規化・ダッキング | ✅ | |
| メタデータ・概要欄テンプレート | ✅ | |
| M4A / WAV 書き出し・共有・保存 | ✅ | |
| Distribution Pack | ✅ | |
| Show Assets | ✅ | |
| 設定（テーマ・言語含む） | ✅ | |
| Podcast RSS 規格の番組・回の情報（データモデル） | ✅ | FR-SHOW-3a / FR-META-5。0005 マイグレーション |
| 配信中の番組の取り込み（検索 / RSS URL / 配信済みの回） | ✅ | FR-SHOW-6〜10。Issue #101。PodsNow の配信基盤への乗り換えの入口 |
| PodsNow の配信基盤からの配信（自分の番組 1 つ） | ✅ | §2.13。配信基盤は Issue #107 |
| 日英ローカライゼーション | ✅ | Issue #80。3 言語目以降は `src/i18n/` にカタログを足すだけ |
| MP3 / FLAC | ❌ 次フェーズ | エンコーダ抽象化の背後に追加 |
| ノイズ除去 / サウンドスタイル / Advanced DSP | ❌ | |
| 編集履歴一覧画面 | ❌ | |
| 複数エピソードテンプレート | ❌ | |
| 文字起こし / ローカル LLM | ❌ | スキーマとインターフェースのみ |
| 同期 / クラウド / 他社配信サービスへの直接アップロード | ❌ | UUID・エクスポート形式で道を残す。他社への直接アップロードは Issue #66（API が無料の場合のみ） |
| 一般向けの配信サービス（アカウント・課金） | ❌ | 自分の番組で配信がうまくいったら判断する（§2.13） |

## 6. 未決事項（実装中に決める）

| # | 項目 | 現在の仮置き |
|---|---|---|
| U-1 | ~~Episode トップ画面の要否~~ | **決着: 持たない。**1 エピソード = 1 画面 + 3 タブ（FR-EP-5） |
| U-2 | 割り込み終了後の自動再開の既定 | 既定は「確認して手動再開」。設定で自動再開を選べる（【仮説】iOS の `shouldResume` オプションに従う） |
| U-3 | 書き出し済みエピソードの元データ自動整理 | 自動削除はしない。設定画面で手動整理 |
| U-4 | ~~ステレオ録音時の書き出しチャンネル~~ | **決着:** Podcast / WAV プリセットはモノラルにミックスダウン（左右の平均）。High Quality とカスタムのステレオは録音・素材の左右をそのまま保つ（AUDIO_DESIGN.md §8.1） |
| U-5 | Spotify for Creators / stand.fm の受け付け形式・サイズ上限 | 【仮説】M4A 受付可と想定。配信前に実サービスで確認 |
| U-6 | MP3 採用時のライセンス（LAME = LGPL） | 次フェーズで判断 |
| U-7 | ~~取り込んだ配信済みの回の最大話数を、新規エピソードの話数の初期値に使うか~~ | **決着: 使う。**配信済みの番号は二度と使わない（§2.1.1）。ユーザー判断（2026-09-25） |
